//! TODO(M0): the `Sidecar` trait and its `run(config)` harness — loads
//! config via `rustak-core`, enrolls/loads the service identity, connects
//! to the stream, drives `on_event`/`tick`, and reports health.
//!
//! See `.claude/plan/plan.md` → Architecture → "Plugin (sidecar) contract"
//! for the full shape; left empty by the workspace-skeleton brief so
//! `rustak-plugin-example` has something to depend on ahead of that brief.

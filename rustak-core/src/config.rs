//! TODO(M0): TOML config loader with ${{ env.X }} interpolation (a FIFO-safe env-file guard), humane duration/listen-address serde adapters.
//!
//! See `.claude/plan/design/01-foundations-storage-ci.md` §2.2 for the
//! exact file breakdown this module grows into; left empty by the
//! workspace-skeleton brief so the crate compiles ahead of that brief.

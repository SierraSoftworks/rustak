# M1-04 — `interop/eud`: commoncommo/commotest CI image

**Goal:** turn the M1-00 recommendation (`.claude/plan/status/M1-00-eud-interop-harness-exploration.md`) into a reproducible CI artefact: `interop/eud/Dockerfile` (+ helper scripts) that clones `TAK-Product-Center/atak-civ` at the pinned commit with a blobless cone-sparse checkout of `commoncommo` + `takthirdparty`, fetches the LFS distfiles over plain HTTPS (no git-lfs), builds `takthirdparty` package-by-package in dependency order (quictls OpenSSL, libxml2, curl, protobuf with the serial host-tools fix, ngtcp2/nghttp3 as required) and then `commoncommo` with `commotest`, and produces a small runtime image containing `commotest` and its shared libs. Add a `nightly.yml` job `interop-eud-image` (`workflow_dispatch` + weekly schedule) that builds the image with buildx, caches layers in ghcr (`ghcr.io/sierrasoftworks/rustak-interop-commoncommo`, tag = atak-civ SHA), and a `interop/eud/README.md` documenting the licence posture (GPL test tool built in CI, nothing vendored), how to run `commotest` scripts locally, and the scenario matrix from M1-00 (what the harness can and cannot cover). Keep the `interop-eud` scenario job as an `if: false` placeholder — scenarios arrive in M2 once rustak can enrol devices.

**Constraints:** no container runtime is available on this machine — you cannot build locally. Write the recipe from the upstream makefiles/docs you can read (`refs` sparse checkout exists in the session scratchpad at `/private/tmp/claude-501/-Users-bpannell-dev-gh-SierraSoftworks-rustak/9ec7cece-906f-4639-9129-d03da7a814b7/scratchpad/refs/atak-civ/commoncommo` and `.../refs/atak-civ` (use `git ls-tree`/`git show HEAD:<path>` for `takthirdparty` files not checked out), and verify every referenced path/target/URL exists (`git ls-tree`, `curl -I` for distfiles). Do not vendor any upstream source or comment text into the repo. Files you own: `interop/eud/**`, `.github/workflows/nightly.yml` (add jobs; keep existing placeholders), `docs/` only if you add `docs/interop.md`. No `git`/`but` writes. Validate YAML with `~/go/bin/actionlint`. Status: `.claude/plan/status/M1-04-interop-eud-image.md` including the exact commit pin, expected build time, and the list of things only a real CI run can confirm.

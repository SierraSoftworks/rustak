# D-01 — Distil `.claude/plan/compat/*.md` from the research reports

**Goal:** write the per-area wire contracts implementation briefs and tests will cite: `compat/streaming.md`, `enrollment.md`, `groups.md`, `contacts.md`, `missions.md`, `files.md`, `profiles.md`, `oauth.md`, `cloudtak.md`. Each file: purpose, the exact request/response shapes (paths, verbs, params with defaults, status codes, `Content-Type`, JSON/XML field names and casing, date formats, envelope `type` strings), the streaming templates where relevant (verbatim XML shapes as *our* renderings, not copied source), ATAK/CloudTAK-specific gotchas, and a "verified in" pointer to the research report section (05/06/07 are verified from source and win over 02; 03 = CloudTAK; 04 = OTS pitfalls only). Keep each file focused and scannable (tables); target 150–400 lines each. Also add `compat/README.md` (index + precedence rules).

**Read:** `plan.md` Appendix A (already distilled — expand it, do not contradict it), research 02–07, `conventions.md`. Do not copy source code or comment text from GPL projects; JSON/XML shapes and paths are facts. Files you own: `.claude/plan/compat/**` only. No `git`/`but` writes. Status: `.claude/plan/status/D-01-compat-docs.md` (list any conflicts between reports you had to resolve and how).

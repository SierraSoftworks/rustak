# M4-02 — Data Sync extras: `<dest mission>` ingest, `t-x-m-*` notifications, invitations, logs, layers, archive, admin API, expiry job

**Read first:** `.claude/plan/conventions.md`; design 04 §4.6 tables for `invitations.rs`, `logs.rs`, `layers.rs` (lines 430–470), §4.7 (`<dest mission>` publish path), §4.8 (notifications), §8.1 Missions row and §8.2 `mission.rs` DTOs (lines 628–656), M4 rows of §10; `.claude/plan/compat/{missions,streaming}.md` (the `t-x-m-*` templates: child **elements** inside `<MissionChange>`, `<mission type name guid authorUid tool [token]>` attributes, `t-x-m-r` uses `type="INVITE"`); research `05` §7 and `06` §7; status `M1-05-stream-server.md` (`MissionIngest`, `Notifier`, `LiveState`, `no_missions()` placeholder in `runtime.rs`/`stream::serve`), `M2-06-marti-groups-contacts.md`, `M4-01-missions-core.md` (the service you extend — start by reading `rustak-server/src/missions/service.rs` as it stands; if M4-01 is still in flight, build your modules against the method names its brief specifies and reconcile before your exit checks).

**Deliver.**
1. `rustak-server/src/missions/cot.rs`: `impl MissionIngest for MissionPublisher` per §4.7 (resolve by name then guid; sender must hold a subscription with `MISSION_WRITE` via client uid → username → cert CN; recipients = connected subscribers minus sender; persist uid with cached details from the event; `ADD_CONTENT`; `t-x-m-c` to recipients). Replace `stream::no_missions()` in `runtime.rs` with the real publisher (one line; re-read the file first).
2. `rustak-server/src/missions/notify.rs` + `stream/notify.rs` mission builders: the `MissionNotice` enum of §4.8 rendered into exact `t-x-m-c[-l|-k|-k-u|-k-c|-m|-e|-h]`, `t-x-m-n`, `t-x-m-d`, `t-x-m-i`, `t-x-m-r` events (one `MissionChange` per `t-x-m-c`; `contentResource` children as elements; `details` as attributes; `stale` +20 s; `how='h-g-i-g-o'`), delivered through the `Notifier` (`Subscribers` → uids, `BroadcastGroups` → readers of the mission's groups minus author). Replace every `TODO(M4-02)` marker M4-01 left with the right notice.
3. `rustak-server/src/missions/{invitations,logs,layers,archive}.rs` + `marti/missions/{invitations,logs,layers}.rs` implementing the §4.6 tables exactly (`GET /missions/invitations?clientUid=` is called by CloudTAK in parallel with the list page and must answer an empty array, never 404; `POST /missions/logs/entries` → 201; layer tree with positions; `DELETE /layers?uid=` unfiles items; map layers stored verbatim; `externaldata`; `feed` no-ops), registered in the explicit order M4-01 established.
4. Admin surface: `web/api/missions.rs` (`GET /missions`, `GET /missions/{guid}`, `DELETE /missions/{guid}?deep`, `GET /missions/{guid}/changes?squashed`, `PUT /missions/{guid}/subscriptions/{clientUid}/role`, `DELETE …/subscriptions/{clientUid}`, `GET /missions/{guid}/archive`) with DTOs in `rustak-api/src/mission.rs` (wasm-safe, round-trip tests, re-exported), audit entries, and `jobs/mission_expiry.rs` (expire + purge soft-deleted after `[retention] missions_purge_after` — add the key to `config/retention.rs` and `config.example.toml`).
5. Tests: `rustak-server/tests/mission_dest.rs` (two fake EUDs via `rustak_client::stream::testing::Eud`: subscriber receives the routed CoT and a `t-x-m-c`; non-subscriber does not; sender lacking WRITE is ignored), goldens for every `t-x-m-*` template, `missions_extras.rs` (invite → `t-x-m-i` → subscribe with the invitation token; logs; layers tree; archive → import into a new mission → equal uids/hashes), admin API tests.

**Files you own:** `rustak-server/src/missions/{cot,notify,invitations,logs,layers,archive}.rs`, `rustak-server/src/marti/missions/{invitations,logs,layers}.rs`, `rustak-server/src/stream/notify.rs` (mission builders only), `rustak-server/src/web/api/missions.rs` (+ route lines in `web/api/mod.rs`), `rustak-api/src/mission.rs` (+ re-export), `rustak-server/src/jobs/mission_expiry.rs` (+ `jobs/mod.rs` line), `config/retention.rs`, `config.example.toml` (`[retention]` block), one line in `runtime.rs`, `rustak-server/tests/{mission_dest,missions_extras}.rs`, your status file. Concurrent agent M4-01 owns the rest of `missions/**` and `marti/missions/**` — re-read `missions/mod.rs`, `missions/service.rs`, `marti/missions/mod.rs`, `marti/mod.rs`, `web/api/mod.rs`, `runtime.rs`, `rustak-api/src/lib.rs` immediately before each edit and keep edits additive. No `git`/`but` writes; files < 300 functional lines. Exit checks as in M4-01, plus the node-tak mission scenarios and the `mission_dest` suite. Status: `.claude/plan/status/M4-02-missions-sync-notify.md`.

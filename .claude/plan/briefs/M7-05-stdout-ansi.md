# M7-05 — No ANSI colour on a non-TTY stdout (upstream option in tracing-batteries, prepared locally)

**Why.** The Nomad deployment reported `docker logs` full of escape codes: rustak's stdout logging carries ANSI colour whether or not stdout is a terminal. `tracing-batteries`' OpenTelemetry battery exposes `with_stdout(bool)` (`../tracing-batteries-rs/src/integration_opentelemetry.rs:238`) but no colour / `NO_COLOR` / `IsTerminal` knob, so the fix starts upstream in the maintainer's own crate, checked out beside this repo at `../tracing-batteries-rs`.

**Read first:** `../tracing-batteries-rs/README.md` and `src/integration_opentelemetry.rs` (how the stdout `fmt` layer is built — find where `with_ansi` could be applied), its `CHANGELOG`/release conventions if any, and its existing tests; `rustak-core/src/telemetry.rs:122` (the one call site); `docs/deployment.md` "Logging and telemetry" section (where the new behaviour is documented).

**Deliver.**
1. **Upstream (in `../tracing-batteries-rs`, working tree only — do NOT commit, push, tag or run any `git`/`but` write there; the maintainer publishes):** add `with_ansi(bool)` to the OpenTelemetry battery (default: `std::io::stdout().is_terminal() && std::env::var_os("NO_COLOR").is_none()` — the `NO_COLOR` convention is "set to any value disables colour"), applied to the stdout fmt layer; a unit test for the default's two inputs (factor the decision into a pure function taking `is_terminal: bool, no_color: Option<OsString>`); README/doc-comment line; a CHANGELOG entry if the repo keeps one. Run that crate's `cargo test` and `cargo clippy --all-targets -- -D warnings`. Report the diff summary and the exact `git diff --stat` in your status note so the maintainer can review and release it.
2. **rustak side, prepared but not applied to `Cargo.toml`:** the one-line change in `rustak-core/src/telemetry.rs` to pass the setting through (an explicit `RUSTAK_LOG_COLOR=always|never|auto` override is welcome if `LOG_LEVEL` already has a home there — keep it to the same file) and the sentence for `docs/deployment.md`. Prove it compiles and behaves by pointing `[patch.crates-io] tracing-batteries = { path = "../tracing-batteries-rs" }` at the local checkout **temporarily**, running `cargo test -p rustak-core`, then **reverting the `Cargo.toml`/`Cargo.lock` patch** — the committed tree must keep the published crate version until a release exists. Leave the telemetry.rs change in place only if it compiles against the *published* version too (feature-detect is not possible, so most likely it cannot); otherwise put the finished change in your status note as a ready-to-apply diff and leave `telemetry.rs` untouched.
3. Status note states plainly: what to release upstream, which version bump, and the follow-up for rustak (bump + apply the diff).

**Files you own:** `../tracing-batteries-rs/**` (uncommitted working-tree changes only), `rustak-core/src/telemetry.rs` (conditionally, see above), `docs/deployment.md` "Logging and telemetry" section (a sentence only if the rustak change lands), your status file. Never leave `Cargo.toml`/`Cargo.lock` modified. Four other agents run in parallel on `rustak-server/**` and `rustak-ui/**`; do not touch those, `.claude/plan/{plan,backlog}.md`, or CI files. No `git`/`but` writes in either repo. Exit checks: upstream crate's `cargo test` + clippy; `cargo fmt --check` here; `cargo test -p rustak-core` against whichever configuration you leave. Status: `.claude/plan/status/M7-05-stdout-ansi.md`.

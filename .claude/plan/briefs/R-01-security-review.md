# R-01 — Independent security review: identity, PKI, enrolment, OAuth, services

**Role.** Reviewer, not implementer. Read-only on the code; you write one report and nothing else. Read `.claude/plan/conventions.md`, `plan.md` → "Identity & auth model (secure by default)" and Appendix A, design 03 §5 threat-model notes, `compat/{enrollment,oauth,cloudtak}.md`, and the status files for M0-11, M2-01, M2-02, M2-03, M2-06, M2-08, M2-10, M2-12, M5-01, M6-01, M0-20. Then read the code: `rustak-server/src/{auth/**,pki/**,identity/**,marti/{tls,enroll,oauth,login,principal}.rs,web/api/{auth,passkey,setup,credentials,certificates,services,events,middleware,extract}.rs,web/helpers/**,plugins/**,crypto/**}`.

**Look for** (with concrete file:line and a reproduction sketch for each finding): authentication bypasses or confused-deputy paths between the three listeners; Basic credentials accepted anywhere beyond `/Marti/api/tls/**` and `/oauth/token`; cookies accepted on `/api/v1`; mission/identity token confusion (D2); PKCE/state/nonce gaps; enrolment-token reuse or race; certificate issuance with a subject the client chose; revocation not enforced at the handshake or not disconnecting sessions; rate-limit gaps on credential endpoints; timing side channels in secret comparison; secrets or CSRs in logs or audit rows; SSE feed leaking events across tenants/channels; service-token scope escalation; sealed-secret misuse (AAD/context); argon2 params reachable from config; anything in `config.example.toml` that defaults insecure. Also check the WebAuthn verifier against the Level 3 RP requirements list in `M0-20`.

**Deliver** `.claude/plan/reviews/R-01-security-review.md`: findings ordered by severity (Critical/High/Medium/Low/Info), each with file:line, why it matters, how to reproduce or a failing-test sketch, and the fix you recommend; then a short "what is done well" section; then a list of tests you would add. No `git`/`but` writes; do not edit source. Return the findings summary.

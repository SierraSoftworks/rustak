# M1-02 — `rustak-cot`: TAK protocol v1 protobuf conversion

**Goal:** complete `rustak-cot/proto/` per `design/02-protocol-streaming.md` §1.1 (the clean-room `.proto` — extend the M0-01 skeleton to the full field table: `TakMessage{takControl=1,cotEvent=2; reserved 3,4}`, `TakControl{minProtoVersion=1,maxProtoVersion=2,contactUid=3,extensionIds=4 repeated}`, `CotEvent` 1–17 incl. `caveat=16`, `releasableTo=17`, `Detail{xmlDetail=1,contact=2,group=3,precisionLocation=4,status=5,takv=6,track=7,extensionDetails=8 repeated {extensionId=1 uint32,data=2 bytes}}`, leaf messages; package `atakmap.commoncommo.protobuf.v1`; your own text only) and §1.2 rows `proto/mod.rs`, `proto/to_proto.rs`, `proto/from_proto.rs`: `event_to_message(&Event) -> TakMessage` with the **strict** extraction rules (typed sub-message only when exactly one element of that name exists with exactly the expected attribute set and no children; whole element stays in `xmlDetail` otherwise; `xmlDetail` = remaining children concatenated via `xml::write_fragment`, omitted when empty; no `Detail` when empty), `message_to_event(TakMessage) -> Result<Event, ConvertError>` (typed elements first unless a same-named element is present in `xmlDetail` — xmlDetail wins; `extensionDetails` kept opaquely on the event; `how == ""` → `None`; times in ms), and `encode/decode` helpers. Tests: design §4 `to_proto`/`from_proto` list (each strict rule, duplicate `<contact>`, bad `battery`, xmlDetail omitted when empty, conflict resolution, missing detail, extension pass-through), hex-golden for the SA fixture, and the proptest round trip modulo documented losses (`tests/roundtrip_prop.rs`).

**Read first:** `.claude/plan/status/M1-01-cot-model-xml.md` (public API: `Event`, `Detail`, `Element`, `StrictDetail`, `xml::{write_fragment,parse_fragment}`, `CotTime`, `ConvertError` already defined), `compat/streaming.md` §protobuf, `plan.md` Appendix A.1 (field numbers), design 02 §0/§1. Facts about TAK Server's converter quirks are in research 05 §12.2 (attribute-count rule) — follow the spec-strict variant the design chose.

**Constraints:** only edit `rustak-cot/proto/**`, `rustak-cot/src/proto/**`, `rustak-cot/tests/roundtrip_prop.rs`, `rustak-cot/Cargo.toml` (workspace deps: `prost`, `bytes`, `proptest` dev) and your status file. Another agent concurrently implements `rustak-cot/src/{codec/**,negotiate.rs,msgs.rs}` and may add a `Frame`/`EncodedEvent` type there — do not touch those; if you need `Event.proto_extensions` (or similar) added to `event.rs`, add the field with a doc comment (coordinate by keeping the change minimal and additive). No `git`/`but` writes; files < 300 functional lines. Exit checks: `cargo test -p rustak-cot`, clippy `-D warnings`, doc `-D warnings`, `cargo fmt -p rustak-cot --check`, `./scripts/check-file-length.sh`, `env -i PATH=/usr/bin:/bin:$HOME/.cargo/bin HOME=$HOME cargo build -p rustak-cot`. Status: `.claude/plan/status/M1-02-cot-protobuf.md`.

# D-02 — Manual compatibility checklists for ATAK, WinTAK and iTAK

**Read first:** `plan.md` → Verification ("Manual checks are reserved for what CI cannot reach"), `docs/interop.md`, `docs/compat/profiles.md` (M3-02's checklist style — match it), research `07` (ATAK behaviour), `compat/*.md`, status M2-09 (what the EUD harness already proves — do not repeat those as manual steps), M4-03 (what the CloudTAK suite proves).

**Deliver** `docs/compat/{atak,wintak,itak,cloudtak}.md`: for each client, a numbered checklist an operator can run in under an hour against a fresh rustak (QR enrolment, manual config package import, channels UI, chat, package send/receive, Data Sync create/subscribe/add marker/file/log, device profile application, disconnect/reconnect, revocation), each step with the expected observable outcome and the rustak log or admin-UI evidence to look for, and a "known gaps" section listing what rustak deliberately does not do (video, federation, QUIC, ExCheck). Link them from `docs/interop.md` and `README.md` (one sentence in "Not included" or Documentation). Facts only; anything you cannot verify from the research goes in as "to confirm". No `git`/`but` writes; Markdown only. Status: `.claude/plan/status/D-02-manual-compat-checklists.md`.

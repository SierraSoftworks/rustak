# M5-01 — OAuth2 authorization-code flow and TAK-style OIDC federation (`/oauth/authorize`, `/login/*`)

**Read first:** `.claude/plan/conventions.md`; design 03 §5 `oauth_server/{authorize,login,state}.rs` (lines 414–432), `oidc/` (433–446), §6 endpoint tables (490–535), §10 M5 steps (681–700); `.claude/plan/compat/oauth.md` and `compat/cloudtak.md` (CloudTAK 13.90 has no OIDC backend route: the goal is TAK-Server parity so it works unchanged when CloudTAK ships it, plus browser SSO for WebTAK-style clients); research `06` §5 (`/login/auth` → IdP → `/login/redirect?code&state` with the sha256 state cookie, `/login/authserver`, `/login/.well-known/openid-configuration`, `/logout`, chunked `access_token_N` cookies); status `M2-03-enrollment-endpoints.md` (`marti/oauth.rs`: password + refresh grants, `token_key`, `jwks`), `M0-11-web-api-auth.md` (`web/helpers/oidc/*`: discovery, JWKS, PKCE+nonce validation; `auth/jwt.rs`), `M2-02-identity-credentials.md` and `identity/groups.rs::apply_claims` (group-claim mapping already exists — reuse it, do not duplicate).

**Deliver.**
1. `rustak-server/src/auth/oauth_server/{authorize,login,state,codes}.rs`: `GET /oauth/authorize` (response_type=code, PKCE S256 required for public clients, registered redirect URIs from `[auth.oauth] clients`, consent-less for first-party clients), `POST /oauth/token` gains `grant_type=authorization_code` (+ `refresh_token` already exists) in `marti/oauth.rs`; server-side pending-auth state in SQLite (`oauth_codes` table, migration `0013`, 10-minute codes, single use, bound to PKCE verifier); `GET /login/auth` → IdP authorization request (state cookie, sha256 compare, nonce), `GET /login/redirect?code&state` → IdP token exchange → JIT user via existing provisioning + `apply_claims` → our JWT set as TAK's chunked `access_token_N` cookies (`Secure`, `HttpOnly`, `SameSite=Lax`, path `/`), `GET /login/authserver`, `GET /login/.well-known/openid-configuration` (`{authorization_endpoint, token_endpoint}`), `GET|POST /logout`. Cookies are accepted **only** on `/login/*` and the Marti routes when a bearer is absent — never on `/api/v1`.
2. Config: `[auth.oauth] clients = [{ id, redirect_uris, public = true }]` with `deny_unknown_fields`, documented in `config.example.toml`; `[auth.oidc]` unchanged.
3. Tests: `TestIdentityProvider` from `testing/oidc.rs` for the federation round trip; authorization-code tests (PKCE mismatch, replay, expired, wrong redirect); cookie scoping tests; a node-tak scenario is not possible (no OIDC in node-tak) — add contract tests asserting the exact bodies TAK Server emits per research 06 §5, and document CloudTAK's future switch in `docs/deployment.md`.

**Files you own:** `rustak-server/src/auth/oauth_server/**` (+ `auth/mod.rs` line), `rustak-server/src/marti/oauth.rs` (additive grant), new `marti/login.rs` (+ one line in `marti/mod.rs`), `config/auth.rs` + `config/validate.rs` (the `[auth.oauth]` block), `migrations/0013_*.sql`, `rustak-server/tests/oauth_flows.rs`, `docs/deployment.md` (SSO section), `.claude/plan/compat/oauth.md` (corrections), your status file. Other agents are editing `web/api/**`, `pki/acme/**`, `stream/**`, `rustak-ui/**` — re-read `auth/mod.rs`, `marti/mod.rs`, `config/validate.rs`, `config.example.toml` immediately before each edit. No `git`/`but` writes; files < 300 functional lines. Exit checks: the standard set. Status: `.claude/plan/status/M5-01-oauth2-authorize-oidc-federation.md`.

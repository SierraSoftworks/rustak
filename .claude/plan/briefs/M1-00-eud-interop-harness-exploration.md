# M1-00 — Exploration: an ATAK-side interop harness that runs in CI

**Question:** how can CI exercise rustak from the *EUD side* using real ATAK code paths (TLS 1.3 + `DEFAULT:!ECDH` handshake, certificate enrollment via `/Marti/api/tls/*`, TAK Protocol v1 negotiation, SA/chat routing, mission-package upload/download, device-profile fetch) without manual testing?

**Candidates to evaluate (in a Docker image built at CI time; GPL code is a test tool, never linked into rustak):**
1. **commoncommo** (ATAK's C++ networking core, `refs/atak-civ/commoncommo/` in the session scratchpad at `/private/tmp/claude-501/-Users-bpannell-dev-gh-SierraSoftworks-rustak/9ec7cece-906f-4639-9129-d03da7a814b7/scratchpad/refs/atak-civ`, and upstream https://github.com/TAK-Product-Center/atak-civ): can `commoncommo/core` (+ `examples/`, e.g. the `commoexample`/`commotest` CLI) be built on Linux (deps: OpenSSL 3, libxml2, curl, protobuf 3.x, ...)? Which example binary drives enrollment (`CommoEnrollment`?), streaming connections, chat, mission packages? What scripting/CLI surface exists? Estimated image build time and size; caching strategy (build once per atak-civ tag, push to ghcr as `rustak-interop-commoncommo`).
2. **ATAK-CIV in an Android emulator** on GitHub Actions (`reactivecircus/android-emulator-runner`): APK availability without a TAK.gov login (F-Droid? GitHub releases of atak-civ ship APKs?), automation via `adb`/`uiautomator`, feasibility of scanning a QR / injecting prefs via `.pref` import, runtime cost. Likely too heavy — quantify.
3. **Third-party clients**: `pytak`/`takproto` (Python), `goatak` (Go), FreeTAKServer's test clients — which cover enrollment + protobuf negotiation faithfully? (They are *not* ATAK code; note fidelity limits.)

**Deliverable** (`.claude/plan/status/M1-00-eud-interop-harness-exploration.md`): a recommendation with a concrete build recipe (Dockerfile sketch, commands, pinned upstream tag), the scenario list the harness can cover vs. cannot, CI cost estimate, licensing note (GPL tool in CI only; nothing vendored into the repo except a Dockerfile that clones upstream), and open risks. Do not write code into the repo; a scratch build attempt in the session scratchpad (`/private/tmp/claude-501/-Users-bpannell-dev-gh-SierraSoftworks-rustak/9ec7cece-906f-4639-9129-d03da7a814b7/scratchpad/interop-explore/`) is encouraged if Docker/toolchains are available locally (check `docker` first; do not install system packages).

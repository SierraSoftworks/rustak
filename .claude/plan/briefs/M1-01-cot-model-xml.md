# M1-01 — `rustak-cot`: event model, XML parse/write, typed details

**Goal:** implement the core of `rustak-cot` per `design/02-protocol-streaming.md` §0 and §1.2 rows `error.rs`, `time.rs`, `types.rs`, `event.rs`, `detail/*` (all typed details incl. `marti.rs` dest handling with first-attribute-wins order, `chat.rs`, `fileshare.rs`, `link.rs`, `mission.rs` skeleton, `flow_tags.rs`, `takcontrol.rs`), `xml/{mod,parse,write}.rs`, plus fixtures and golden/property tests (design §1.2 "Fixtures and tests" and §4 unit list for `xml::*`, `time`, typed details). Protobuf conversion, codecs, negotiation and control messages are **separate briefs (M1-02/M1-03)** — leave `proto/`, `codec/`, `negotiate.rs`, `msgs.rs` untouched (keep the existing `proto/mod.rs` from M0-01 compiling).

**Read first:** `.claude/plan/conventions.md`; `plan.md` → Appendix A.1 (verified wire facts); design 02 §0, §1, §4, §6; research `05-takserver-streaming-auth-verified.md` §4–§5 (framing/serialisation facts, control-message drop rules), `07-atak-client-verified.md` §3.5, §7.2–7.4 (chat/fileshare attribute sets, endpoint sentinels), `03-cloudtak-node-tak-contract.md` §4.2 (never self-closing `<event/>`, no control chars). Fixtures must be our own (no copied captures).

**Files you own:** everything under `rustak-cot/` except `proto/**` and `build.rs`. Add deps via `workspace = true` (`quick-xml`, `memchr`, `thiserror`, `chrono`, `bytes`; dev: `proptest`, `rstest`, `pretty_assertions`). No `git`/`but` writes; files < 300 functional lines.

**Exit checks:** `cargo test -p rustak-cot`, `cargo clippy -p rustak-cot --all-targets -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc -p rustak-cot --no-deps`, `cargo fmt --all --check`, `./scripts/check-file-length.sh`, and `env -i PATH=/usr/bin:/bin:$HOME/.cargo/bin HOME=$HOME cargo build -p rustak-cot` (no protoc). Status: `.claude/plan/status/M1-01-cot-model-xml.md` (document the public API surface — `Event`, `Detail`, `Element`, `TypedDetail`, `CotTime`, `xml::{parse,write}` — so M1-02/M1-03 can build on it without reading the code).

# M0-17 — Project docs: deployment, contributing, README

**Goal:** `docs/deployment.md` (single binary + `config.toml` walkthrough using `config.example.toml`; Docker/`docker-compose.yml` example running `ghcr.io/sierrasoftworks/rustak` with a `/data` volume and ports 8446/443, 8443, 8089; a CloudTAK compose snippet pointing `url/api/webtak` at rustak with the CA trusted via `NODE_EXTRA_CA_CERTS`; systemd unit; TLS modes (`internal`, `files`, `acme` — ACME arrives in M2, say so), first-run setup token flow, passkey/OIDC sign-in, backup (database + `.key` + `pki/` + `content/` + `streams/`)), `CONTRIBUTING.md` (workspace layout, build order `trunk build` → `cargo build`, local checks incl. `scripts/check-file-length.sh`, the <300-line rule, testing conventions, `--features testing`, GitButler branch/commit conventions from `.claude/plan/conventions.md`, licensing rule about GPL references), and expand `README.md` (what rustak is, feature status table per milestone with current state from `.claude/plan/status/`, quick start, links to docs and `.claude/plan/`). Also update `docs/ci.md` "Nightly interop" to match `nightly.yml` (three jobs) as flagged in `.claude/plan/status/M1-04-interop-eud-image.md`.

**Constraints:** docs only: `docs/**`, `CONTRIBUTING.md`, `README.md`. Read `config.example.toml`, `docs/ci.md`, `docs/interop.md`, `docs/plugins.md`, the status files, and `plan.md` for accuracy; never claim a feature works that no status file verifies. No `git`/`but` writes. Status: `.claude/plan/status/M0-17-docs.md`.

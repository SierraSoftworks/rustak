# M2-10 — ACME certificates for the public listener

**Read first:** `.claude/plan/conventions.md`; design 03 §3 `acme/` (lines 265–289), §8 config sketch for `[acme]` (lines 551–645), §10 (M2 steps), §11 risks; `rustak-server/src/config/acme.rs` and `config.example.toml` `[acme]` (keys already exist: `enabled`, `directory` (`letsencrypt` | `letsencrypt-staging` | URL), `email`, `domains`, `challenge` (`tls-alpn-01` | `http-01`), …); `rustak-server/src/web/tls.rs` (today `mode = "acme"` is refused with a message); `pki/tls/resolver.rs` (the `ResolvesServerCert` the public listener already uses for hot reload), `crypto/` (`Sealed` for the account key), `db/migrations/0003_auth_pki.sql` (`acme_accounts`, `acme_certificates` tables), `jobs/` (host + registered jobs), status `M2-01-pki-issuance.md`, `M0-12-runtime-bootstrap.md`.

**Deliver.**
1. `rustak-server/src/pki/acme/{mod,account,order,challenge,renew}.rs` on `instant-acme` (current stable): account creation/reuse (key sealed in the secret store, row in `acme_accounts`), order → authorisations → challenge → finalise → download chain; **TLS-ALPN-01** through the existing rustls resolver (serve the challenge certificate for the `acme-tls/1` ALPN on `:443`) and **HTTP-01** through a tiny route on the public listener (`/.well-known/acme-challenge/{token}`, only while a challenge is pending); the issued key+chain sealed at rest and stored in `acme_certificates`; the resolver swapped without a restart.
2. `web/tls.rs`: `mode = "acme"` now builds the public listener with the last issued certificate if one exists, otherwise the internal-CA certificate as a bootstrap so the listener can come up and complete the first order; validation already requires `:443`/`:80` to be bound — keep it and improve the message.
3. `jobs/acme_renew.rs`: hourly check, renew at 30 days before expiry (configurable), exponential back-off on failure with audit entries and a `WARN` that names the reason; a `POST /api/v1/settings/tls/renew` admin trigger and `GET /api/v1/settings/tls` showing the current certificate, source and next renewal (DTO in `rustak-api/src/settings.rs`).
4. Tests: unit tests for order state machine and challenge material; an integration test against **Pebble** is not available offline, so use `instant-acme`'s types with a `wiremock`-backed directory covering account creation, one HTTP-01 order and a renewal decision; document the manual staging check in `docs/deployment.md` (already describes the mode; update it).

**Files you own:** `rustak-server/src/pki/acme/**` (+ `pki/mod.rs` line), `rustak-server/src/web/tls.rs`, `rustak-server/src/pki/tls/resolver.rs` (additive), `rustak-server/src/jobs/acme_renew.rs` (+ `jobs/mod.rs` line), `rustak-server/src/web/api/settings.rs` (the TLS routes) and one line in `web/api/mod.rs`, `rustak-api/src/settings.rs`, `config/acme.rs`, `config/validate.rs` (ACME rules only), `docs/deployment.md` (ACME section), `Cargo.toml` (add `instant-acme` to `[workspace.dependencies]`), your status file. Other agents are editing `web/api/{services,packages,clients,cot}.rs`, `auth/oauth_server/**`, `stream/router.rs`, `rustak-ui/**` — re-read `web/api/mod.rs`, `jobs/mod.rs`, `pki/mod.rs`, `Cargo.toml` immediately before each edit. No `git`/`but` writes; files < 300 functional lines. Exit checks: the standard set from `conventions.md` plus `cargo run -p rustak-server -- --config config.example.toml --check` and a config with `mode = "acme"` + `listen = [":443"]` passing `--check`. Status: `.claude/plan/status/M2-10-acme.md`.

# M2-09 — EUD interop scenarios: drive `commotest` against rustak in CI

**Read first:** `.claude/plan/conventions.md`; status `M1-00-eud-interop-harness-exploration.md` §3 (the `commotest` CLI, `estream:`/`sstream:`/`chatsend:`/`smpsend:`/`remiface`/`quit`, the exact `commo-log.txt` lines and `commo-xml.txt` semantics, the enrollment-mode mapping — **ATAK sends the token as the Basic password**, `<eport>` = 8446) and §3.3/§4 (night-one scenario list and coverage matrix); `interop/eud/README.md` (licence posture: process boundary and text files only — never vendor, link or include anything from atak-civ); `.claude/plan/compat/{streaming,enrollment,files}.md`; status `M2-05-interop-node-tak.md` and `interop/node-tak/src/{rustak,bootstrap,surfaces}.ts` (server launcher, `/api/v1` bootstrap, surface probing — reuse them); `.github/workflows/nightly.yml` (the `interop-eud-image` job publishes `ghcr.io/sierrasoftworks/rustak-interop-commoncommo:atak-civ-<short-sha>` and `:latest`; the `interop-eud` job is an `if: false` placeholder you replace). The image build is green as of nightly run 35359416075.

**Deliver.**
1. `interop/eud/scenarios/*.toml` — declarative scenarios (rustak config overrides, per-EUD `commotest` argv with placeholders `{host}`, `{stream_port}`, `{enroll_port}`, `{username}`, `{token}`, `{truststore}`, expected `commo-log.txt` regexes with ordering, expected/forbidden `commo-xml.txt` events by uid/type, minimum run time) and `interop/eud/src/*.ts` — a TypeScript runner (`npm test`), sharing code with node-tak by moving the launcher/bootstrap into `interop/shared/` (adjust node-tak's imports; keep its suite green) rather than copying. The runner: starts rustak with a scratch data dir and a test CA, bootstraps an admin and mints per-EUD accounts + one-time enrollment tokens through `/api/v1`, exports the CA as a PKCS#12 truststore for `estream:`, runs each EUD as `docker run --rm --network host -v <out>:/work <image> <argv>` (image from `RUSTAK_EUD_IMAGE`, default the `:latest` tag), waits for exit, then asserts on the two files **after** the process ends (M1-00 §3.2 caveat) and on rustak's side through `/api/v1` and Marti reads. Missing server surfaces are probed and reported as skips with the flipping brief named, exactly like node-tak does.
2. Night-one scenarios, in this order: `enroll-basic` (token → `signClient/v2` → stream up → `Proto Negotiate … accepted` → SA visible via `/Marti/api/clientEndPoints` or `/api/v1/clients` if present → no `Interface Error` for 90 s → issued `commo-enroll-cert.p12` has `CN=<username>` first), `two-eud-routing` (two EUDs in disjoint IN/OUT channels via `/api/v1/users/{u}/groups`; assert what each sees in `commo-xml.txt`), `chat-direct` (`chatsend` A→B delivered; B absent → `b-t-f-s` back to A), `disconnect` (`remiface` on A → B receives `t-x-d-d` with `<link uid=A>`), `negotiate-refused` and `negotiate-silent` (add a **test-only** knob `[stream] negotiation = "accept" | "refuse" | "silent"` in `rustak-server/src/config/stream.rs` + `stream/negotiation.rs`, default `accept`, documented in `config.example.toml` as a compatibility-testing switch; assert `using xml only` / the 60 s timeout line and that XML still flows), `enroll-revoked` (skip until `POST /api/v1/certificates/{id}/revoke` exists — M2-08), `mp-upload` (`smpsend`; skip until `/Marti/sync/missionupload` exists — M3-01; when present assert `result = SUCCESS` and the three-step server sequence in the audit log), `mp-download` (skip until M3-01).
3. CI: replace the `interop-eud` placeholder in `.github/workflows/nightly.yml` with a job on `ubuntu-latest` that builds `rustak-ui` (release) + `rustak-server` (debug, cached like the other jobs), pulls the image tag derived from the pin exactly as `interop-eud-image` does (fall back to `:latest`), runs `npm ci && npm test` in `interop/eud`, and uploads `commo-log.txt`/`commo-xml.txt`/rustak logs as artifacts on failure. Keep it nightly + `workflow_dispatch`; do not make it depend on rebuilding the image. Update `interop/eud/README.md` ("Scenarios" section) and `docs/interop.md`.
4. Tests without Docker: unit tests for the scenario loader, argv templating, the log/xml parsers and the assertion engine using fixture files written from the exact format strings in M1-00 §3.2 (`interop/eud/fixtures/`). Docker is **not** available on this machine, so the end-to-end path is verified by dispatching the nightly workflow: after your files are landed by the orchestrator, the orchestrator dispatches it; you record in the status file exactly which scenarios you expect to pass, skip and fail on first run and why.

**Files you own:** `interop/eud/**` (not the image scripts — `Dockerfile`, `fetch-*.sh`, `build-takthirdparty.sh`, `stage-runtime.sh` stay as they are), `interop/shared/**` (new), `interop/node-tak/src/*.ts` (import path changes only; the suite must stay 16/25 passing), `.github/workflows/nightly.yml` (the `interop-eud` job only), `rustak-server/src/config/stream.rs` + `rustak-server/src/stream/negotiation.rs` (the knob only — other agents own the rest of `rustak-server`; re-read before editing and keep the edit minimal), `config.example.toml` (`[stream]` block only), `docs/interop.md`, your status file. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cd interop/eud && npm run typecheck && npm test` (unit tests + the Docker-less "probe only" mode must pass; the runner must fail loudly and clearly when `docker` is missing and `RUSTAK_EUD_REQUIRE_DOCKER=1`), `cd interop/node-tak && npm run typecheck && npm test` (16/25 with rustak built), `cargo test -p rustak-server --features testing -- stream::negotiation config::stream`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all --check`, `./scripts/check-file-length.sh`, `actionlint` if installed. Status: `.claude/plan/status/M2-09-eud-interop-scenarios.md`.

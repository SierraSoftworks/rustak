# M3-05 — Base URLs under HTTP/2 (`:authority`), and the p256 0.14 bump

**Read first:** `.claude/plan/status/CI-01-2026-09-18-mission-package-url.md` (the CI steward's write-up: `marti/sync_read.rs::content_url()` falls back to `request_base_url()`, which returns `None` for every HTTP/2 client because `base_url_from` requires a literal `host` header while h2 carries the authority in the `:authority` pseudo-header; the last resort is then `https://{server.name}` — a display name — so ATAK handed its peer an unresolvable package URL and `mp-download` failed with 0 of 1052 bytes), `rustak-server/src/web/helpers/request.rs`, `web/api/passkey.rs` (~line 363, the same blind spot for the WebAuthn origin), `config/server.rs` (`base_url`, `domains`, `name`), `.claude/plan/conventions.md`.

**Deliver.**
1. `web/helpers/request.rs`: derive the authority from the `host` header **or** `request.uri().authority()` (actix exposes h2's `:authority` there), honouring the existing trust-proxy/forwarded handling; the last resort order becomes `[server] base_url` → first of `[server] domains` → `server.name`, never a bare display name when a domain is configured. Unit tests for an h2-style request (no `host`, authority on the URI), an h1 request, forwarded headers, and the fallbacks.
2. Route every caller through it: `marti/sync_read.rs::content_url()`, `marti/sync.rs` (missionupload URL), `web/api/passkey.rs` origin/RP-ID derivation, and any other `request_base_url`/`base_url_from` user (grep). Add a regression test in `tests/sync_contract.rs` that issues an HTTP/2 request (actix test client with `http2` or a raw h2 client via `reqwest` with `http2_prior_knowledge`) and asserts the returned URL's host.
3. **p256 0.14**: bump `p256` in the workspace `Cargo.toml` to `0.14.x` (Dependabot #3), fix `rustak-server/src/testing/authenticator/keys.rs` (`to_encoded_point(false)` → `to_sec1_point(false)` per the steward) and anything else the bump breaks; keep `webauthn_rp`'s own p256 version whatever it pins (check `cargo tree -d` for duplicates and say so).

**Files you own:** `rustak-server/src/web/helpers/request.rs`, the call sites named above (minimal edits), `rustak-server/src/testing/authenticator/keys.rs`, `Cargo.toml` (the `p256` line), `Cargo.lock`, `rustak-server/tests/sync_contract.rs` (additive), your status file. Seven other agents are editing concurrently (`web/api/{packages,clients,cot,settings,services,events}.rs`, `pki/acme/**`, `auth/oauth_server/**`, `stream/**`, `rustak-client/**`, `rustak-ui/**`, and `Cargo.toml` for `instant-acme`); re-read `Cargo.toml` and any call-site file immediately before editing and keep edits to the lines you need. No `git`/`but` writes; files < 300 functional lines. Exit checks: the standard set plus `cd interop/node-tak && npm test` (24/25 or better). Status: `.claude/plan/status/M3-05-http2-authority-and-p256.md`.

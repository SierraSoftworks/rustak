# M1-09 — Robustness fixes from R-03: storage, HTTP client, jobs, zip work (non-stream items)

**Read first:** `.claude/plan/reviews/R-03-robustness-review.md` (findings with file:line, scenario and fix — this brief covers **C2, H3, H4, H5, H6, M1, M3, M4, M5, M6, M8, L1, L2, L6**; the stream-side items C1, H1, H2, M2, M7 are a separate brief because another agent owns `stream/**` right now), `.claude/plan/conventions.md`, status M0-07, M0-09, M0-10, M1-05, M3-01, M3-02, M3-03, M0-19.

**Deliver, each with the test the review sketches (or a cheaper deterministic one):**
- **C2** `cot_store/history.rs`: raise `MAX_OPEN_LOGS` to a configurable `[storage] open_history_logs` (default 4096) with an LRU that seals **without** creating a new segment on reopen when the last segment is still unsealed (reopen the unsealed segment for append instead — `append_log::open` already repairs); the 30-minute/500-device soak the review proposes becomes a fast deterministic test asserting segment count stays ≤ devices × 2 under round-robin access.
- **H3** `services/mod.rs`: `reqwest` client with connect (10 s) and total (30 s) timeouts and a response-size cap where `.json()` is used (OIDC discovery/JWKS, ACME, token exchange); a wiremock test with a stalled body.
- **H4** `cot_store/query.rs`: apply `limit` (and a hard cap of 10 000) while walking segments, newest first; test with a large synthetic history asserting decode count.
- **H5** new `jobs/content_orphans.rs`: the collector `[retention] content_orphans` documents — sweep `ContentStore::iter()` against `resources`, `profile_files`, `mission_contents`, `acme_certificates` (grep for every table storing a hash), respect the grace period, remove `tmp/*` older than the grace period; plus cleanup on the error paths in `store/content.rs`; tests for referenced/unreferenced/in-grace.
- **H6** `marti/profiles.rs` (and mission import/export, PKCS#12 building): run zip/deflate on `spawn_blocking`; cache the built profile package per (profile set, updated-max) in memory with invalidation on profile change; test that a second request does not rebuild.
- **M1** mission-package import: an explicit body limit from `[marti] upload_size_limit` and a streamed read into a temp file rather than `to_vec()`.
- **M3** `jobs/**`: max attempts + dead-letter state, log a panicking job with its name, bound concurrent jobs (`[jobs] concurrency`, default 4).
- **M4** `runtime.rs`/`stream/mod.rs` ordering only if trivial: stop the CoT store **after** the stream drain within the budget (coordinate: `stream/mod.rs` belongs to the stream brief — if the fix needs it, write it down for that brief instead).
- **M5** `auth/ratelimit.rs`: O(1) check with periodic sweep; buckets freed on lockout expiry; a criterion-free micro-benchmark test asserting bounded time at 100k buckets.
- **M6/M8** `store/append_log.rs`, `segment.rs`: register a created segment before writing (or remove on crash at open), and cap `forget()` at the frame limit.
- **L1/L2/L6**: migration version read inside the transaction; `Database::close` without the failing `Arc::try_unwrap`; start-up `std::fs` moved to `tokio::fs` or `spawn_blocking`.

**Files you own:** `rustak-server/src/{cot_store/**,store/**,files/**,jobs/**,services/mod.rs,marti/profiles.rs,profiles/**,missions/{import,archive}.rs,auth/ratelimit.rs,db/{connection,migrations}.rs,config/{storage,retention,marti,server}.rs}`, `config.example.toml` (those blocks), tests, your status file. Another agent (M4-04) owns `marti/missions/**`, `missions/**` except the two files above, `marti/{contacts,cot,sync,sync_read,tls}.rs`, `stream/**`, `identity/groups.rs`, `config/validate.rs`; the CI steward edits tests/CI; a security fix agent may soon own `auth/**` except `ratelimit.rs`, `pki/**`, `web/api/**`. Re-read shared files (`jobs/mod.rs`, `config.example.toml`, `services/mod.rs`) immediately before editing. No `git`/`but` writes; files < 300 functional lines. Exit checks: the standard set. Status: `.claude/plan/status/M1-09-robustness-storage-jobs.md`.

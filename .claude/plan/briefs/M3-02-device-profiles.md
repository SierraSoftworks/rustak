# M3-02 — Device profiles, `.pref` writer, package builders, config packages

**Read first:** `.claude/plan/conventions.md`; design 04 §6 (lines 553–601), §5.3 (manifest reader/writer, lines 542–552), §8.1 Profiles + Config packages rows and §8.2 `profile.rs`/`config_package.rs` DTOs (lines 628–656); `.claude/plan/compat/profiles.md`, `compat/enrollment.md`; research `07` §2.5/§6 (ATAK `.pref` and package import rules — `class` attribute literal `class java.lang.String|Boolean|Integer|Long|Float`, `cot_streams` keys, `.p12` re-homing) and `06` §10; status `M2-01-pki-issuance.md` (`pki::p12::{client_keystore,truststore}`), `M2-03-enrollment-endpoints.md` (`GET /Marti/api/tls/profile/enrollment` and `/device/profile/*` currently answer **204 stubs in `marti/tls.rs`** — take those routes over and delete the stubs), `M2-04-marti-foundations.md`, `M0-07-*` (migration `0007_profiles.sql` already has `profiles`/`profile_files`; add `0009_*.sql` for `profile_prefs` and any missing columns, with migration tests).

**Deliver.**
1. `rustak-server/src/files/package.rs`: Mission Package manifest reader/writer per §5.3 (tolerant reader: `Configuration` needs `name`, mint `uid` if missing; `Content` valid iff `zipEntry` non-empty; entries relative to the directory containing `MANIFEST/`). Pure-Rust `zip` (deflate) only.
2. `rustak-server/src/profiles/{model,prefs,builder,config_package,service}.rs` (+ `mod.rs`): the `.pref` renderer (`<?xml version='1.0' standalone='yes'?>`, `preference version="1" name=…`, exact `class` strings, deterministic order, `APP_PREFS = com.atakmap.app.civ_preferences` parameterised), `enrollment_defaults(host, user)` (`deviceProfileEnableOnConnect`, `displayServerConnectionWidget`, `prefs_enable_channels`, `prefs_enable_channels_host-<host>` as the String `"true"`, optional callsign/team/role), `build_profile_package` (`fileN/<name>` + `MANIFEST/manifest.xml` with `uid`, `name`, `onReceiveImport=true`, `onReceiveDelete=true`), `build_multifile_package`, and the manual config packages `build_wintak_atak` (double-wrapped `<user>_CONFIG.zip`) and `build_itak` (flat) with the `cot_streams` entries from §6.3 (cert variant and enrol variant), using `pki::p12` for the truststore/client keystore. A `ProfileService` over the repos: list/get/create/update/delete, files (content in `ContentStore`), prefs, and the delivery queries (enrollment, connection with `syncSecago`, tool, file by `relativePath`) with group matching and `Last-Modified`.
3. `rustak-server/src/marti/profiles.rs` implementing the §6.4 table exactly (204 / 200 zip / 304 via `If-Modified-Since`; `relativePath` traversal rejected; single file ⇒ raw bytes with guessed `Content-Type`), registered in `marti/mod.rs` with one `.configure` line (and `.default_service()` on any nested scope). The admin `/Marti/api/device/profile*` wrappers may answer 501 for now except list/get.
4. `/api/v1` admin surface: `web/api/profiles.rs` (`GET/POST /profiles`, `GET/PATCH/DELETE /profiles/{id}`, files upload/list/delete, `GET/PUT /profiles/{id}/prefs`, `GET /profiles/{id}/preview`, `GET /profiles/pref-catalog` with a small curated catalogue) and `web/api/config_packages.rs` (`POST /config-packages {username, credential_id?, variant, include_client_cert}` ⇒ zip download; it consumes an existing `ClientPassword`/certificate and never mints one), with DTOs in `rustak-api/src/{profile,config_package}.rs` (wasm-safe, round-trip tests) re-exported from `rustak-api/src/lib.rs`, and audit entries.
5. Tests: renderer goldens (byte-exact `.pref`), manifest round-trips, zip layout assertions (entry names, manifest parameters), 204/200/304 and `relativePath` behaviour in `rustak-server/tests/profiles_contract.rs`, admin API tests. ATAK/WinTAK/iTAK import remains a manual gate — record the checklist in `docs/compat/profiles.md` (create it) and say so in the status file.

**Files you own:** `rustak-server/src/profiles/**`, `rustak-server/src/files/package.rs` (+ `pub mod package;` line in `files/mod.rs` — M3-01 creates `files/mod.rs`; if it does not exist yet when you need it, create it with only that line and tell M3-01 in your status file), `rustak-server/src/marti/profiles.rs`, the profile/enrollment-profile routes in `marti/tls.rs` (removal of the 204 stubs only), one line in `marti/mod.rs`, `rustak-server/src/web/api/{profiles,config_packages}.rs` (+ route lines in `web/api/mod.rs`), `rustak-api/src/{profile,config_package}.rs` (+ re-exports), `rustak-server/migrations/0009_*.sql`, `rustak-server/tests/profiles_contract.rs`, `docs/compat/profiles.md`, `.claude/plan/compat/profiles.md` (corrections), your status file. Concurrent agents: M3-01 owns `files/{store,metadata,legacy,search}.rs`, `marti/{sync,sync_metadata,files}.rs`, migration `0008`; M2-06 owns `marti/{groups,contacts,subscriptions}.rs`; M2-07 owns `rustak-ui/**` and may add re-exports to `rustak-api/src/lib.rs` — re-read shared files (`marti/mod.rs`, `web/api/mod.rs`, `rustak-api/src/lib.rs`, `files/mod.rs`) immediately before each edit. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cargo test -p rustak-server --features testing`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings` (no features), `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps`, `cargo fmt --all --check`, `./scripts/check-file-length.sh`, `cargo run -p rustak-server -- --config config.example.toml --check`. Status: `.claude/plan/status/M3-02-device-profiles.md`.

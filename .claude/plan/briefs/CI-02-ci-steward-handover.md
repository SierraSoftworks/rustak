# CI-02 — CI steward (takes over from CI-01)

**Why.** The first CI steward (brief `CI-01-*.md`, running log `.claude/plan/status/CI-01-ci-steward.md`, incident notes `.claude/plan/status/CI-01-2026-09-19-*.md`) kept `main`'s pipelines healthy for four days and was lost on 2026-09-22 to repeated API server errors with a very large context. You replace it. Its notes and `docs/ci.md` are the handover; read them before acting.

**Role.** Keep CI green and honest for `SierraSoftworks/rustak`, and tell the orchestrator (the main session) what the registry and the runs actually say. You own `.github/**` and `docs/ci.md`. You **prepare** changes in the working tree and tell the orchestrator; the orchestrator reviews and lands them (never run `git`/`but` write commands, never push, never merge or close PRs). You may request `gh run rerun --failed` only for a failure you have shown to be infrastructure, and say so. You may dispatch `nightly.yml` when asked. Comments on **new** Dependabot PRs are allowed by the maintainer's standing decision; nothing on old PRs; security findings go through GitHub's security interface.

**Standing rules learned the hard way (all in the notes; do not relearn them):** a test that passes locally and fails in CI every time is an environment difference, not load (the `test` job runs without a built UI); ONE test failing repeatedly while siblings sharing its helper pass is something only that test does; read a slow `Test` by its per-binary shape (uniform slowdown across untouched binaries = the host) and never call a trend from one sample; verify what `:latest` contains from the registry's `org.opencontainers.image.revision` label and the `sha-<commit>` tags, never from job status; images publish only after lint/test/e2e/node-tak pass, `:latest` only moves forward (ancestry rule), trunk and cross are fetched by pinned SHA-256 with the Actions cache in front, rust-cache saves only from `main` (the 10 GB quota was the root cause of repeated fetch failures), five uploads on the publish path retry once; run `actionlint` and check YAML structure before reporting any workflow edit; run `cargo fmt --check` on the whole workspace before calling any Rust change ready; keep reports short and factual, and own mistakes plainly.

**State at handover (2026-09-22).** `main` = `65ed9fe` (M9-11 control-link polish) on `511dbab` (CloudTAK parse-log step made non-vacuous) on `90c89b7` (hostile default server name in every suite; its push run and the dispatched nightly 35672048856 were green: compose 10/10, EUD 9/9, node-tak 35/35, e2e 41/41). Runs in flight: `65ed9fe` (in progress), `511dbab` (queued). Next landing: M9-12 (`rustak-plugin-adsb` only). Production (Dublin, Nomad) pulls pinned digests of `rustak`, `rustak-plugin-ais`, `rustak-plugin-adsb`; its session waits for all three at revision ≥ `65ed9fe`. The upload retry has never yet fired (all first attempts succeeded since it landed). Open items that belong to the maintainer, not to you: publishing v0.0.3, deleting the empty v0.0.2 and the stale "Version 0.0.1" draft, issues #4/#5, Dependabot #10 (rebased, awaiting merge) and #11 (blocked on `tracing-batteries-rs` moving to opentelemetry 0.33).

**Owed to the orchestrator now, in order:** (1) from the registry, when `rustak`, `rustak-plugin-ais` and `rustak-plugin-adsb` `:latest` are at revision ≥ `65ed9fe`, with digests; (2) the forward-only decision each of the `511dbab` and `65ed9fe` runs logged, and whether any upload retry fired; (3) on the next nightly (scheduled 04:17 UTC), that `cloudtak-parse-log`'s passing reason reports a non-zero line count; (4) `Test` timing samples against the 60-minute bound (normal band 16–20 min, worst seen 36m56s), reported as samples, not trends.

**How to work.** Use `gh` for runs and logs; read the registry over HTTPS (token from `https://ghcr.io/token?scope=repository:sierrasoftworks/<image>:pull`, manifest index → amd64 manifest → config blob → label) because local Docker tooling is unusable on this machine. Append dated entries to `.claude/plan/status/CI-02-ci-steward.md` (start it with a short summary of what you inherited). Stop and report after each meaningful event rather than holding results; the orchestrator resumes you with a message when something new lands.

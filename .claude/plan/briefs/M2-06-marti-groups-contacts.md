# M2-06 — Marti groups/channels, contacts, clientEndPoints, subscriptions

**Goal:** implement `rustak-server/src/marti/{groups,contacts,subscriptions}.rs` per `compat/groups.md`, `compat/contacts.md` and design 04 §3, backed by `identity::members` (grants + per-device active state), the stream `Hub`/`LiveState` snapshot and `Notifier` from M1-05: `GET /Marti/api/groups/all?useCache&sendLatestSA` (envelope `type` `com.bbn.marti.remote.groups.Group`; both IN and OUT grants with `active`, `bitpos` always set, `created` `yyyy-MM-dd`, `type` `SYSTEM`; `sendLatestSA=true` → `Notifier::resend_latest_sa`), `PUT /Marti/api/groups/active[?clientUid]` (bare JSON array body — accept numeric `created` — update per-device active state (all of the user's devices when `clientUid` absent), re-auth live sessions (`LiveState::reauth_user`), `t-x-g-c` to the user's other devices, 200 empty), `PUT /groups/activebits`, `GET /groups/groupCacheEnabled` (true), `GET /groups/{name}/{direction}`, `GET /groups/user?username` (admin), `GET /users/all` (admin); `GET /Marti/api/contacts/all` (bare array `{filterGroups, notes, callsign, team, role, takv, uid}` from live subscriptions the caller can reach, `notes` = username), `GET /Marti/api/clientEndPoints?secAgo&showCurrentlyConnectedClients&showMostRecentOnly&group…` (`ClientEndpoint` fields incl. `lastStatus` `Connected|Disconnected`, `lastEventTime` unpadded-millis format; disconnected devices from the `devices` table with `last_seen_at`; `group` filter 403 when not readable), `GET /Marti/api/subscriptions/all` (`SubscriptionInfo` with every key present), `GET /Marti/api/subscription/{uid}`, `POST /Marti/api/subscriptions/incognito/{uid}`, `DELETE /Marti/api/subscriptions/delete/{uid}` (admin), filter endpoints as no-ops. Also emit `t-x-g-c` from M2-02's `set_active_groups`/`users_groups::put` TODOs via the `Notifier`. Contract tests per design 04 §9 rows Groups and Contacts (ATAK parser rules: `bitpos ≥ 0`, mandatory `name/direction/type/created`; CloudTAK `Group` type; `lastStatus` values; bare array for contacts), plus integration tests with two fake EUDs (`rustak-client` `Eud`) asserting `t-x-g-c` delivery and latest-SA resend.

**Read first:** conventions; `compat/groups.md`, `compat/contacts.md`, `compat/cloudtak.md`; design 04 §3, §9; status files M1-05 (Hub snapshot, `LiveState`, `Notifier` API), M2-02 (members/devices services), M2-04 (marti response helpers, extractors, `kind` constants), M2-03 (principal resolution / listener policies).

**Constraints:** only edit `rustak-server/src/marti/{groups,contacts,subscriptions}.rs` + registration lines in `marti/mod.rs`, `rustak-server/src/identity/{members,devices}.rs` (only to call the `Notifier` where the TODOs are), `rustak-server/src/web/api/{devices,users_groups}.rs` (same), tests, and your status file. No `git`/`but` writes; files < 300 functional lines. Exit checks as M2-03. Status: `.claude/plan/status/M2-06-marti-groups-contacts.md`.

## Addendum (after M1-05 landed)

- **Wire `LiveState` onto the context.** M1-05 (`.claude/plan/status/M1-05-stream-server.md`, "LiveState is not on AppContext") left the live-connection handle unreachable from handlers. Add `Late<crate::stream::LiveState>` beside `Late<PkiAuthority>` in `rustak-server/src/services/mod.rs` (`install_live`, `live() -> Result<Arc<LiveState>, Error>`, `has_live()`), and install it from `runtime::listen` right after the stream runtime binds (`StreamRuntime::live()`; when the stream listener is off, do not install it and have the endpoints answer empty lists rather than 500). Use `snapshot()`/`snapshot_for(&Principal)` for `/Marti/api/clientEndPoints` and `/Marti/api/contacts/all`, and `notifier().groups_changed(...)` where `PUT /Marti/api/groups/active` and the `/api/v1` channels admin routes carry `TODO(M2-08)` markers (`identity/members.rs`, `web/api/users_groups.rs`) — those `t-x-g-c` notices are yours now.
- `runtime.rs`, `web/server.rs`, `services/mod.rs` are shared with nobody else at the time you start; keep edits small and re-read before editing anyway.
- Every nested Marti scope needs its own `.default_service()` (M2-04 finding), and the `[stream.limits]`/`stream::` types are stable API from M1-05 — do not modify `stream/**`; if a snapshot field is missing, add a tiny accessor and say so.

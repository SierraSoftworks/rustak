# M4-01 — Data Sync core: mission model, roles, tokens, CRUD, subscriptions, contents, changes

**Read first:** `.claude/plan/conventions.md`; design 04 §0 (D1–D14), §4.1–§4.6 (lines 271–470: schema intent, roles, `MissionService`, `auth/mission_token.rs`, exact wire DTOs, endpoint tables for `crud.rs`, `misc.rs`, `contents.rs`, `changes.rs`, `subscription.rs`) and the M4 rows of §10; `.claude/plan/compat/missions.md`, `compat/cloudtak.md`; research `06` §7–§8 (authoritative for any gap); status `M2-04-marti-foundations.md`, `M2-03-enrollment-endpoints.md`, `M2-06-marti-groups-contacts.md` (`LiveState` on the context), `M3-01-enterprise-sync-files.md` (`files::*`, resources repo, `ContentStore`), `M1-05-stream-server.md` (`MissionIngest`/`Notifier` seams — this brief does **not** implement them; M4-02 does), `M0-07-*` (migration `0006_missions.sql` already defines `missions`, `mission_subscriptions`, `mission_changes`, `mission_contents`, `mission_uids`, `mission_layers`, `mission_logs`, `mission_invitations`; add `0010_*.sql` only for what is missing — e.g. cached uid details, layer positions, `mission_external_data`, `map_layers` — with migration tests).

**Deliver.**
1. `rustak-server/src/missions/{model,roles,service,contents,changes,subscriptions,keywords,dto}.rs` (+ `mod.rs`) and `db/repos/missions*.rs`: the `MissionService` surface of §4.3 for the areas above (list/resolve/role resolution/create-or-update/delete with archive-to-resource + soft delete/copy/add-remove content/import package via `files::package`/keywords/changes with the **squashed fold** of D11/`cot_events_xml`/subscribe-unsubscribe-set_role-access_token-set_password). Mission password hashing uses argon2id like the rest of rustak (design says bcrypt for TAK parity; parity is not required because the hash never leaves the server — document this).
2. `rustak-server/src/auth/mission_token.rs`: HS256 mission tokens (`SUBSCRIPTION|INVITATION|ACCESS`, `MISSION_NAME`, `MISSION_GUID`, `jti`, `iat`, optional `exp`) keyed by a dedicated 32-byte secret sealed in the `SecretStore` (`mission-token-hmac`), `MissionAuthorization`-then-`Authorization` header precedence with the D2 rule (a bearer that verifies as our RS256 identity JWT is never a mission token). Wire the header precedence through `marti/principal.rs` only if it is not already there.
3. `rustak-server/src/marti/missions/{mod,crud,misc,contents,changes,subscription}.rs`: the §4.6 tables for those files, byte-exact DTOs (`Mission` arrays always present, `passwordProtected`/`inviteOnly` always, `token`+`ownerRole` only on 201 create, `MissionSubscription` 201 with token, `API_VERSION` handling, UUID-shaped names rejected, `DELETE /missions?guid=`), registered from `marti/mod.rs` with **explicit ordered registration** so `/missions/all/...`, `/missions/logs/...`, `/missions/guid/...` and `/missions/invitations` are never parsed as names (leave the `logs`, `invitations`, `layers`, `all/*` routes to M4-02 but reserve the names and add the ordering test). Every nested scope carries `.default_service()`.
4. Notifications and the `<dest mission>` publish path are M4-02's; expose the hooks they need (`MissionService::changes_since`, `subscribers_for`, `role_of`) and leave `TODO(M4-02)` markers where a `t-x-m-*` notice must be emitted — nothing else.
5. Tests: goldens for `Mission`, `MissionChange`, `MissionSubscription`, `Resource` JSON (transcribe the shapes from node-tak's TypeBox types in `interop/node-tak/node_modules/@tak-ps/node-tak` as a JSON-schema oracle in `rustak-server/tests/fixtures/`), a proptest comparing the squashed fold against a naive replay model, the route-ordering test, token round-trips, `rustak-server/tests/missions_flow.rs` (create → token/guid → subscribe → contents add/remove → changes squashed/full → password/access token → delete by guid → 410). The node-tak `missions.test.ts` scenarios must flip to passing: `cd interop/node-tak && npm test` after `cargo build -p rustak-server` and `cd rustak-ui && trunk build`; paste the summary.

**Files you own:** `rustak-server/src/missions/**`, `rustak-server/src/auth/mission_token.rs` (+ its `auth/mod.rs` line), `rustak-server/src/marti/missions/{mod,crud,misc,contents,changes,subscription}.rs`, one registration line in `marti/mod.rs`, `rustak-server/src/db/repos/missions*.rs` (+ `mod.rs` lines), `rustak-server/migrations/0010_*.sql`, `rustak-server/tests/{missions_flow.rs,fixtures/**}`, `.claude/plan/compat/missions.md` (corrections), your status file. Concurrent agent M4-02 owns `marti/missions/{invitations,logs,layers}.rs`, `missions/{notify,cot,invitations,logs,layers,archive}.rs`, `stream/notify.rs` mission builders, and `web/api/missions.rs` — agree on the `MissionService` method names by reading each other's status files, re-read shared files (`marti/mod.rs`, `marti/missions/mod.rs`, `missions/mod.rs`, `auth/mod.rs`) immediately before each edit. No `git`/`but` writes; files < 300 functional lines (route files parse parameters and call the service; parameter structs live in `dto.rs`). Exit checks: `cargo test -p rustak-server --features testing`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings` (no features), `RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps`, `cargo fmt --all --check`, `./scripts/check-file-length.sh`, node-tak summary. Status: `.claude/plan/status/M4-01-missions-core.md`.

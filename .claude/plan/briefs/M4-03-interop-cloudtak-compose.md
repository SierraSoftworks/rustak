# M4-03 — `interop/cloudtak`: CloudTAK full-stack compose suite (nightly)

**Read first:** `.claude/plan/conventions.md`; `plan.md` → Verification → `interop/cloudtak/`; `.claude/plan/research/03-cloudtak-node-tak-contract.md` (CloudTAK's server config, login, channels, data sync, packages API; its `NODE_EXTRA_CA_CERTS` trust; the three URLs), `compat/cloudtak.md`; status `M2-05-interop-node-tak.md` and `M2-09-eud-interop-scenarios.md` (`interop/shared` launcher/bootstrap/probe — reuse), `docs/interop.md`; CloudTAK's own `docker-compose.yml` in the reference checkout under the session scratchpad `refs/CloudTAK` (read for facts: services, env vars, ports, health endpoints) — nothing from it is copied, only the compose you write.

**Deliver.**
1. `interop/cloudtak/docker-compose.yml` (Postgres + CloudTAK API/UI at the pinned image tag + rustak built from this checkout, sharing a network; rustak with `[web.public.tls] mode = "files"` using a test CA whose root is handed to CloudTAK via `NODE_EXTRA_CA_CERTS`), `interop/cloudtak/src/*.ts` runner driven through **CloudTAK's REST API**: create the CloudTAK admin, configure the TAK server connection (`url=ssl://rustak:8089`, `api=https://rustak:8443`, `webtak=https://rustak:8446`), log in with a rustak client password, channels list/toggle, create a Data Sync mission, add a marker and a file, read changes, share a package; plus a Playwright smoke of the CloudTAK UI (login page, map loads, mission list) — assertions at the API level, screenshots as artefacts.
2. `.github/workflows/nightly.yml`: replace the `interop-cloudtak` placeholder with a job that builds rustak's image locally (the existing Dockerfile with `dist/` from a `cargo build --release`), runs the compose, waits for health, runs the suite, uploads logs and screenshots on failure; nightly + `workflow_dispatch` + a `run-cloudtak` PR label. Coordinate on `nightly.yml`: the CI steward may edit other jobs — re-read before editing and touch only your job.
3. Docker is not available on this machine: unit-test the runner's request builders and parsers with fixtures, make `npm test` fail loudly without Docker unless `RUSTAK_INTEROP_REQUIRE_DOCKER=0`, and record in your status file exactly what you expect the first CI run to do. Update `docs/interop.md`.

**Files you own:** `interop/cloudtak/**`, the `interop-cloudtak` job in `.github/workflows/nightly.yml`, `docs/interop.md` (CloudTAK section), `.claude/plan/compat/cloudtak.md` (corrections), your status file. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cd interop/cloudtak && npm run typecheck && npm test`, `actionlint` if installed, `./scripts/check-file-length.sh`. Status: `.claude/plan/status/M4-03-interop-cloudtak-compose.md`.

# M2-04 — Marti API foundations: envelope, errors, extractors, version/config, stubs

**Goal:** create `rustak-server/src/marti/{mod,response,error,extract,headers,time,version,stubs}.rs` per `design/04-marti-api-missions-files-profiles.md` §1–§2 and `compat/cloudtak.md` + `compat/contacts.md` (version parts): `ApiResponse<T>` envelope with the per-endpoint `type` constants (`response::kind`), one JSON helper that sets `Content-Type: application/json` **exactly** (no charset) and legacy `text/json`/`text/plain`/`application/xml` helpers, `MartiError` → `{status, code, message}` JSON with the design's mapping table plus the two fixed HTML bodies for container-level errors, `ApiVersion` header extractor (`API_VERSION`, default 2), `MissionRef` name/GUID sniffing, `CommaList<T>`, `LooseBool`, case-insensitive query lookup, `MartiHeaders` (HSTS when TLS, CORS allow-all when configured, never 3xx — disable actix path normalisation redirects), `time.rs` (`cot_date` padded millis, `group_date` `yyyy-MM-dd`, `java_date_string`, `TimeWindow::parse(secago,start,end)` with the 24 h cap flag); endpoints: `GET /Marti/api/version` (text `TAK Server rustak-<semver>`), `/Marti/api/version/config` (`{"version":"3","type":"ServerConfig","data":{version, api:"3", hostname}}`), `/Marti/api/version/info`, `/Marti/api/node/id`, `GET /files/api/config` (`{"uploadSizeLimit": <int MB from [storage] or a new [marti] upload_size_limit_mb>}`), `/Marti/api/util/user/roles` (bare array; `ROLE_READONLY` when no IN group), `/Marti/api/util/isAdmin`, `/Marti/api/home`, `GET /Marti/GetTime`, `POST /Marti/ErrorLog` (accept, store size-capped in kv or discard with audit), and stubs (`GET /Marti/api/video` → `{"videoConnections":[]}`, `/Marti/vcm` empty XML, `/Marti/api/injectors/cot/uid` + `/Marti/api/repeater/list` empty lists, KML servlets and `/Marti/sync/missioncreate` → 501 JSON). Mount the `marti` scope on the public listener (`web/server.rs::services()`) behind the existing bearer-based `Principal` resolution for now (cert/Basic resolution arrives with M2-03 — leave a clearly named seam `marti::auth_policy(ListenerRole)`); add `pub mod marti;` to `lib.rs`. Contract tests: route table asserting exact `Content-Type` per route, envelope `type` strings, no 3xx on trailing-slash/wrong-method probes, error JSON mapping, date formatters (unit), `TimeWindow` parsing, version/config JSON golden matching ATAK's `ServerVersion` parser expectations (`version` int-parsable, `type == "ServerConfig"`, `data.version` string).

**Read first:** conventions; `compat/cloudtak.md`, `compat/README.md`, `compat/contacts.md` (version/roles bits), `compat/missions.md` §envelope; design 04 §0 (D1–D14), §1, §2, §9 (test plan rows for content-type/envelope/errors); status files M0-11 (web app builder, `Principal`, extractors, audit), M0-12 (runtime), M0-06 (config; add a `[marti]` section only if needed — keep it minimal: `public_host`, `upload_size_limit_mb`).

**Constraints:** only edit `rustak-server/src/marti/**`, `rustak-server/src/lib.rs` (one line), `rustak-server/src/web/server.rs` (mount only), `rustak-server/src/config/marti.rs` + registration in `config/mod.rs` (if you add the section) and `config.example.toml` (document it), tests under `rustak-server/tests/marti_*.rs`, and your status file. Another agent is finishing `pki/**`; another writes `e2e/**`; another `rustak-client/**`. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cargo test -p rustak-server --features testing marti::` + your integration tests, `cargo clippy -p rustak-server --all-targets --all-features -- -D warnings` (report pki lints as pre-existing if they remain), doc, `cargo fmt -p rustak-server --check`, `./scripts/check-file-length.sh`. Status: `.claude/plan/status/M2-04-marti-foundations.md`.

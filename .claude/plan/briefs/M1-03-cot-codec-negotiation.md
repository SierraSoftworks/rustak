# M1-03 — `rustak-cot`: frame codecs, negotiation and control messages

**Goal:** implement `rustak-cot/src/codec/{mod,varint,xml_frame,proto_frame,encoded}.rs`, `negotiate.rs`, `msgs.rs` per `design/02-protocol-streaming.md` §1.2 rows of the same names (**exclude** `codec/auth.rs` — the plan removed the TCP `<auth>` path): `Mode {Xml, Proto}`, `Frame {Xml(Bytes), Proto(Bytes)}`, `TakCodec` implementing `tokio_util::codec::{Decoder, Encoder<Frame>, Encoder<&EncodedEvent>}` with `set_mode`; `XmlScanner` (find `<event` followed by space/`>`/tab/newline, discard bytes before it, split after `</event>`, `Oversized` past the cap); `ProtoScanner` (skip to `0xBF`, LEB128 length, resync on bad varint or oversize, return payload); `varint` put/read; `EncodedEvent` (lazy XML/proto bytes via `OnceLock`, `frame(mode)`) — for the proto side depend on `crate::proto::event_to_message` + `encode` which a concurrent brief (M1-02) is finishing; if not yet present when you compile, define the call against the signatures in `design 02 §1.2` and coordinate via `cargo check` retries, do not implement conversion yourself; `negotiate.rs` (`announce`, `parse_announce`, `request`, `parse_request`, `response`, `parse_response` with the exact templates from `compat/streaming.md`: `t-x-takp-v` with `<TakControl><TakProtocolSupport version="1"/><TakServerVersionInfo serverVersion apiVersion/></TakControl>`, `how=m-g`, point 0/0 ce/le 999999, stale +60 s; request reuses uid; response `<TakResponse status="true|false"/>`); `msgs.rs` (`ping` uid `<uid>-ping`, `pong` uid `takPong` no detail, `disconnect` `t-x-d-d` with `<link relation="p-p" uid type/>`, `group_change` `t-x-g-c`, `incognito_toggle`, `is_ping`/`is_pong`). Tests per design §4: varint boundaries; xml_frame chunking/garbage/split/oversize/declaration/`\r` tolerance; proto_frame partial varint, 11-byte varint resync, oversize resync, garbage skip count, zero-length payload, 0xBF inside payload, split payload; `TakCodec` mode switch through `Framed` over `tokio::io::duplex`; template equality tests for negotiate/msgs.

**Read first:** `.claude/plan/status/M1-01-cot-model-xml.md` (public API), `compat/streaming.md`, `plan.md` Appendix A.1, design 02 §0, §1.2, §4; research 05 §3–§5 (server-side templates; verified). Facts only, no copied text.

**Constraints:** only edit `rustak-cot/src/{codec/**,negotiate.rs,msgs.rs}`, `rustak-cot/tests/codec_framed.rs`, `rustak-cot/Cargo.toml` (workspace deps: `tokio-util` codec, `bytes`, `memchr`; dev `tokio` macros/rt/io-util) and your status file. Another agent concurrently owns `rustak-cot/proto/**`, `src/proto/**`, `tests/roundtrip_prop.rs`. No `git`/`but` writes; files < 300 functional lines. Exit checks as in M1-02 plus `cargo test -p rustak-cot --test codec_framed`. Status: `.claude/plan/status/M1-03-cot-codec-negotiation.md`.

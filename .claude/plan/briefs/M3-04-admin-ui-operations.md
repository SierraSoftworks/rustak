# M3-04 — Admin UI: profiles, missions, config packages, settings (wave A); packages, clients, CoT browser (wave B)

**Read first:** `.claude/plan/conventions.md`; design 04 §8.3 (lines 657–662); status `M2-07-admin-ui-identity.md` (the page/component/api-client/fixture patterns to follow exactly, the QR component, `confirm`, `groups_picker`), `M3-02-device-profiles.md` (`/api/v1/profiles*`, `/config-packages`, DTOs in `rustak-api/src/{profile,config_package}.rs`), `M4-02-missions-sync-notify.md` (`/api/v1/missions*` and `rustak-api/src/mission.rs`), `M2-08-identity-api-gaps.md` (certificates endpoints — add a certificate fingerprint/expiry/revoke to the device tab while you are there).

**Wave A (APIs exist on `main`):** `pages/profiles.rs` + `pages/profile_editor.rs` (flags, groups picker, tool, `PrefsEditor` with class select and the pref catalogue autocomplete, files upload/delete, preview download), `pages/missions.rs` + `pages/mission_detail.rs` (Overview / Subscribers with role dropdown and remove / Changes with squashed toggle / Layers tree / Delete with confirm / Download archive), a **config package** action on the user detail page (variant, credential picker, download), `pages/settings.rs` additions for TLS/ACME status once `GET /api/v1/settings/tls` exists (feature-detect: hide the card on 404), and the certificate details/revoke on the Devices tab. Components: `prefs_editor.rs`, `role_badge.rs`, `file_drop.rs`, `xml_view.rs`. Demo fixtures for every page; Playwright specs for profiles (create → prefs → preview) and missions (list → detail → delete) against the real API.

**Wave B (after M3-03 lands; re-read `rustak-api` then):** `pages/packages.rs` (upload drop zone, groups chips, tool, install-on-enrolment toggle, delete, download), `pages/clients.rs` (live list, 5 s auto-refresh, disconnect, incognito), `pages/cot_browser.rs` (latest per uid with filters, detail drawer with `XmlView` + history). Start wave B only when `.claude/plan/status/M3-03-admin-api-packages-clients-cot.md` exists.

**Files you own:** `rustak-ui/**`, `e2e/tests/**`, your status file. Never touch `rustak-server/**`; if an endpoint is missing or wrong, record it precisely in your status file. No `git`/`but` writes; files < 300 functional lines. Exit checks as in M2-07 (`trunk build`, wasm clippy `-D warnings`, `cargo test` in `rustak-ui`, `cargo fmt --check`, file-length, `cd e2e && npm run typecheck && npx playwright test` with the `RUSTAK_E2E_CHROMIUM` path from M2-07). Status: `.claude/plan/status/M3-04-admin-ui-operations.md` (write it after wave A and update after wave B).

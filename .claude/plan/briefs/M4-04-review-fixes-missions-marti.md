# M4-04 — Fix the Critical/High findings of the wire-compatibility review (R-02)

**Read first:** `.claude/plan/reviews/R-02-wire-compat-review.md` (the findings, with file:line, citations and the tests it proposes — implement its Critical and High items and the Medium items marked here), `.claude/plan/conventions.md`, `compat/{missions,streaming,contacts,files}.md`, research `05`/`06`, status M4-01, M4-02, M2-06, M1-05, M3-03 (`cot_store::query` is the read path to reuse for the Marti CoT routes).

**Deliver, in this order (each with tests):**
1. **Mission authorization matrix.** Every `/Marti/api/missions/**` route resolves its role through the shared `allowed(...)` helper; `GET …/subscription?uid=` returns the **stored** subscription (never mints), `DELETE …/subscription`, and `GET|DELETE /missions/logs/entries/{id}` check roles. Add the anonymous-and-wrong-role authorization matrix test over every mission route the review proposes (table-driven, one assertion per route × {anonymous, other-mission token, read-only subscriber, owner}).
2. **`/Marti/api/cot/**`** per design 04 §7 (lines 602–627): `GET /cot/xml/{uid}` (single `<event>` with XML header, `<marti>` stripped, 404 when empty), `GET /cot/xml/{uid}/all?secago|start|end`, `GET|POST /cot` (uids body) and `GET /cot/sa?start&end&left&bottom&right&top` (≤24 h) → `<events>`, `GET /cot/matchUid?search` bare string array — all from `cot_store::query`, group-filtered by the caller's OUT channels via the stored sender bits, mounted on both listeners with `.default_service()`; the stream's oversize `b-f-t-r` pointer (`stream/writer.rs`) now targets a route that exists and uses the server-generated template (`stale=+100s`, research 05 §9; no unconditional `<ackrequest>`).
3. **`clientEndPoints` leak**: visibility check before the per-account memo in `marti/contacts.rs`; test with an invisible account holding two devices.
4. **`<dest mission>` reachability**: the raw relay applies the IN/OUT rule like every other delivery (`stream/dest.rs`/`router.rs`); test with subscribers in disjoint channels.
5. Mediums explicitly in scope: OIDC suffix stripping at the **first** `_READ`/`_WRITE` occurrence per TAK (`identity/groups.rs`); `missionupload` honours `keyword=`; `t-x-g-c` only when the active set actually changed; `tls/config` never advertises a `nameEntry` the issuer drops and unknown keys are refused at config load; `?offset=N` on `/Marti/sync/content` answers `200` with the partial body as TAK does (keep `206` for a real `Range` header).
6. **Compat doc corrections** the review lists (flow tag `TAK-Server-{id}` is correct in code — fix `streaming.md` §8; `files.md` §9 client vs server `b-f-t-r`; settle `missions.md` §3 vs §4 on `boundingPolygon` from research 06 and say which won; the `<role>` shape: research 05 §7.5 is authoritative over design 04 — change the renderer to repeated `<permissions>` elements if that is what §7.5 shows, and update M4-02's deviation note).

**Files you own:** `rustak-server/src/marti/missions/**`, `missions/**`, `marti/{contacts,cot,sync,sync_read,tls}.rs` (+ one `cot` registration line in `marti/mod.rs`), `stream/{dest,router,writer,mission_notify,mission_payload}.rs`, `identity/groups.rs`, `config/validate.rs` (nameEntry rule), `rustak-server/tests/{missions_authz.rs (new),marti_cot.rs (new)}` and additive cases in existing suites, `.claude/plan/compat/*.md`, `.claude/plan/status/M4-02-missions-sync-notify.md` (the deviation note only), your status file. Another fix agent may soon own `auth/**`, `pki/**`, `web/api/**` from the security review — stay out of those. No `git`/`but` writes; files < 300 functional lines. Exit checks: the standard set plus `cd interop/node-tak && npm test` (25/25) and `cd interop/eud && npm test` (unit). Status: `.claude/plan/status/M4-04-review-fixes-missions-marti.md`.

# R-02 — Independent compatibility review: wire contracts against the verified research

**Role.** Reviewer, not implementer. Read-only on the code; you write one report. Read `.claude/plan/research/{05,06,07}*.md` (authoritative), `03` (CloudTAK/node-tak), `compat/*.md`, and the status files for M1-05, M1-08, M2-03, M2-04, M2-06, M3-01, M3-02, M4-01, M4-02, M2-09, M4-03. Then the code: `rustak-cot/src/**`, `rustak-server/src/{stream/**,marti/**,missions/**,files/**,profiles/**,cot_store/**}`.

**Compare byte-level contracts:** CoT XML output (declaration, no trailing newline, never self-closing `<event/>`), protobuf framing and negotiation timing, ping/pong template, `t-x-d-d`/`t-x-g-c`/`t-x-m-*` templates (elements vs attributes), flow tags, `<marti><dest>` precedence and reachability rule, `b-t-f-s`; Marti envelope `type` strings, date formats per endpoint (padded millis vs `.S'Z'` vs `yyyy-MM-dd` vs Java `Date.toString()`), `Content-Type: application/json` exactness, never-3xx, `resultCount`, legacy Title-case Metadata, `Files` map-of-strings, `Group` fields (`bitpos ≥ 0`, `created` date-only), `clientEndPoints` `lastStatus`, `MissionSubscription` 201 + token, `Mission` arrays always present, `MissionChange` shape, mission-package `MANIFEST/manifest.xml`, `.pref` `class` literals and `cot_streams` keys, `tls/config` root and `nameEntry` rules, `signClient/v2` JSON vs XML, `/oauth/token` body and JWT header/claims for node-tak, `/files/api/config`. Flag every divergence with file:line, the research citation, the client that would break (ATAK, CloudTAK, WinTAK, iTAK) and the severity; note deliberate deviations recorded in `compat/*.md` as accepted only if the rationale holds.

**Deliver** `.claude/plan/reviews/R-02-wire-compat-review.md` ordered by client impact, plus a list of contract tests you would add and the manual checks that remain necessary. No `git`/`but` writes; do not edit source. Return the findings summary.

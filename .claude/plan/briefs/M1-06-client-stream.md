# M1-06 — `rustak-client::stream`: TAK stream client (TLS, negotiation, keepalive, reconnect, fake EUD)

**Goal:** implement `rustak-client/src/stream/{mod,connect_string,tls,connection,keepalive,reconnect,testing}.rs` per `design/02-protocol-streaming.md` §3 with the plan delta **no `<auth>` credentials / no plain TCP in production** — the client still supports `tcp://` *only* behind a `#[cfg(any(test, feature = "testing"))]`/`insecure-tcp` feature so server integration tests can run without TLS when needed; default builds expose TLS only. `Endpoint` parsing accepts `host:port:ssl` (ATAK) and `ssl://host:port` (CloudTAK) (`tcp` only with the feature). `TlsIdentity` from PEM files (truststore, chain, key) with `rustls` client config (client auth cert); `TakStream` implementing `Stream<Item = Result<Event, StreamError>>` + `Sink<Event>` over `Framed<_, TakCodec>` with the client-side negotiation state machine (on `t-x-takp-v` with version 1 → send `request`, buffer outbound until `t-x-takp-r`; `true` → `set_mode(Proto)` both ways + flush; `false`/60 s timeout → XML forever), `pass_control` flag, `server_version()`; keepalive per ATAK constants (ping after 15 s idle, repeat 4.5 s, dead at 25 s → `RxTimeout`); `Reconnecting` wrapper with backoff 1–30 s and an on-connect hook; `testing.rs` `Eud` helper (`sa(lat, lon)`, `send`, `expect(pred, timeout)`, `expect_none`) behind the `testing` feature for `rustak-server` integration tests and future interop suites. Use `rustak_cot::{codec::TakCodec, negotiate, msgs, Event}` (see `.claude/plan/status/M1-03-cot-codec-negotiation.md` and `M1-01-cot-model-xml.md` for the exact API — `TakCodec` never returns stream-ending errors; read its drop counters).

**Read first:** conventions; `compat/streaming.md`; design 02 §3 and §4 (client tests: connect string, negotiation state machine via `tokio::io::duplex`, keepalive timing with `tokio::time::pause`); status files M1-01/M1-02/M1-03; `rustak-client/src/sidecar/**` (M0-15) to expose `SidecarEvent::Cot(Event)` wiring later — do not rewire the sidecar in this brief beyond adding a `stream` module and re-exports.

**Constraints:** only edit `rustak-client/**` (add deps via `workspace = true`: `rustls`, `tokio-rustls`, `rustls-pki-types`, `rustls-pemfile`, `tokio-util`, `futures`, `bytes`, `tokio-stream`; features `testing`, `insecure-tcp`) and your status file. Another agent may be editing `rustak-server/**` — do not touch it. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cargo test -p rustak-client --all-features`, `cargo clippy -p rustak-client --all-targets --all-features -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc -p rustak-client --no-deps`, `cargo fmt -p rustak-client --check`, `./scripts/check-file-length.sh`. Status: `.claude/plan/status/M1-06-client-stream.md` (document the `TakStream`/`Eud` API for the server-side integration-test brief).

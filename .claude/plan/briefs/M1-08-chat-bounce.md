# M1-08 — Bounce undeliverable GeoChat (`b-t-f` → `b-t-f-s`) and audit the control-message gaps

**Read first:** `.claude/plan/conventions.md`; `.claude/plan/compat/streaming.md` (§ routing: "undeliverable `b-t-f` ⇒ `b-t-f-s` back to sender") and research `05` §5 (the exact `b-t-f-s` template TAK Server sends: uid, `<detail>` with `__chat`/`chatgrp`/`link` echoed, `how`, stale); status `M1-05-stream-server.md` (router, `dest.rs`, `notify.rs` builders, `EncodedEvent`), `M2-09-eud-interop-scenarios.md` (the `chat-direct` scenario that expects the bounce and why it fails today); `rustak-cot/src/msgs.rs` (a `b-t-f-s` builder may already exist).

**Deliver.**
1. In `rustak-server/src/stream/router.rs` (+ `dest.rs` if the resolution lives there): when an inbound `b-t-f` carries `<marti><dest callsign=…|uid=…>` and **no** recipient is reachable (unknown callsign/uid, or reachable set empty after IN/OUT), send the sender a `b-t-f-s` built by `rustak_cot::msgs` (add the builder there if missing, with a golden test) and count it in `stream/metrics.rs`. Broadcast chat (no dest) is never bounced. Do not bounce when at least one recipient received it.
2. While there, audit the control-type list against `compat/streaming.md` (`t-b*`, `t-x-c-f`, `t-x-c-t(-r)`, `t-x-takp-q`, `t-x-c-m`, `t-x-c-i-e/-d`) and confirm each is consumed, never relayed; add a table-driven test if one is missing. Confirm `t-x-c-i-e/-d` (incognito) actually toggles the subscription's incognito flag so `/Marti/api/contacts/all` and SA replay hide it.
3. Tests: a `stream_routing` case for the bounce (two fake EUDs, unknown callsign ⇒ sender receives `b-t-f-s` with the original `__chat@id`; known callsign ⇒ no bounce), plus the golden. Update `interop/eud/scenarios/chat-direct.toml` expectations only if the template differs from what the scenario asserts (say so in the status file).

**Files you own:** `rustak-server/src/stream/{router,dest,control,metrics}.rs`, `rustak-cot/src/msgs.rs`, `rustak-server/tests/stream_routing.rs`, `interop/eud/scenarios/chat-direct.toml`, `.claude/plan/compat/streaming.md` (corrections), your status file. Other agents may be editing `stream/notify.rs`, `stream/mission_notify.rs`, `missions/**`, `marti/**`, `web/api/**`, `testing/**` — do not touch those. No `git`/`but` writes; files < 300 functional lines. Exit checks: `cargo test -p rustak-cot`, `cargo test -p rustak-server --features testing -- stream`, `cargo test -p rustak-server --features testing --test stream_routing`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all --check`, `./scripts/check-file-length.sh`. Status: `.claude/plan/status/M1-08-chat-bounce.md`.

# M2-05 — `interop/node-tak`: CloudTAK-library contract suite (scaffold + first scenarios)

**Goal:** create `interop/node-tak/` — a TypeScript test project using `@tak-ps/node-tak` and `@tak-ps/node-cot` exactly as CloudTAK does — with a runner that (1) starts `rustak` from `target/{debug,release}/rustak` with a scratch config (TLS `internal`, ports on localhost, setup token pre-seeded, `localhost` base URL), (2) bootstraps through `/api/v1`: setup admin (passkey ceremony is browser-only — use the setup/registration token path to obtain an admin bearer if M0-11 exposes one, otherwise document the gap and use a `RUSTAK_TEST_ADMIN_TOKEN` env var the server can accept in `testing` builds — check `.claude/plan/status/M0-11-web-api-auth.md`/`M0-12` first), creates a user, mints a **client password**, (3) trusts `<data_dir>/pki/ca.crt` via `NODE_EXTRA_CA_CERTS`, and (4) runs scenarios with `node:test`: `version.test.ts` (`GET /Marti/api/version` via mTLS once a cert exists; until then bearer), `login.test.ts` (`POST /oauth/token` password grant → node-tak `OAuth.parse` on the JWT), `enrollment.test.ts` (`Credentials.generate()` flow: `tls/config` XML + `signClient/v2` JSON), `groups.test.ts`, `contacts.test.ts`, `stream.test.ts` (connect `ssl://`, expect `t-x-c-t-r` after ping, `t-x-takp-v` optional), `missions.test.ts`, `files.test.ts`. Scenarios whose endpoints don't exist yet must be `skip`ped with a `TODO(M2-03|M3|M4)` reason, not deleted. Add the CI job body for `interop-node-tak` in `.github/workflows/rust.yml` (replace the placeholder: needs the release UI bundle + debug server build, `npm ci`, run the suite; keep `if: false` until M2-03 lands — leave a comment saying what flips it). Document in `interop/node-tak/README.md` (how CloudTAK's contract is exercised, how to run locally, env vars).

**Read first:** conventions; `compat/cloudtak.md`, `compat/oauth.md`, `compat/enrollment.md`, `compat/groups.md`, `compat/contacts.md`, `compat/missions.md`, `compat/files.md`, `compat/streaming.md`; research `03-cloudtak-node-tak-contract.md` (node-tak API: `TAKAPI.init`, `APIAuthPassword`, `APIAuthCertificate`, `Credentials.generate`, `Group.list/update`, `Contacts.list`, `Mission.*`, `Files.*`, `TAK.connect`); status files M0-11/M0-12/M2-02 (what exists: `/api/v1/auth/*`, credentials mint API); `e2e/scripts/start-server.mjs` for the server-launch pattern. The node-tak sources are cloned at `/private/tmp/claude-501/-Users-bpannell-dev-gh-SierraSoftworks-rustak/9ec7cece-906f-4639-9129-d03da7a814b7/scratchpad/refs/node-tak` (MIT) for API reference.

**Constraints:** only edit `interop/node-tak/**`, the `interop-node-tak` job in `.github/workflows/rust.yml`, `docs/interop.md` (one paragraph), and your status file. No `git`/`but` writes. `npm ci`/`npm install` is allowed inside `interop/node-tak` (commit `package-lock.json`). Exit checks: `npm test` runs locally with the skipped scenarios reporting as skipped and the runnable ones (version via bearer, login → JWT parse, groups/contacts if present) passing against a locally built `rustak` (`cargo build -p rustak-server` first; UI bundle via `cd rustak-ui && trunk build`), `npx tsc --noEmit`, `~/go/bin/actionlint` on `rust.yml`. Status: `.claude/plan/status/M2-05-interop-node-tak.md` listing what passes today and what flips each skip.

# Backlog — small follow-ups recorded by the orchestrator

Items too small for a brief of their own, or waiting for a milestone. Remove a line when it lands.

M7 (2026-09-19) landed M7-01 plain_bind + ACME handles + wildcard refusal, M7-02 sweep job, M7-03 OIDC channel cache, M7-04 UI tests + demo TLS; M7-05 stdout ANSI is prepared upstream (see below).

- **`/oauth/userinfo` releases the full claim set to every live token** rather than narrowing by the OIDC scopes granted at authorization: the scopes are recorded on the code, which is spent in seconds, and a rustak access token has nowhere flat to carry them. Not a widening (`GET /api/v1/me` answers the same facts to the same token); the ID token *is* narrowed. Fix: record the granted scopes against the session/`jti` and read them at userinfo. Found by M8-01.
- **AIS: Digitraffic (Finland) source is documented but not built.** It needs MQTT over WebSocket (`rumqttc` with its websocket feature brings a second WebSocket and TLS stack); the README lists the endpoint, topics and CC BY 4.0 licence. Build it when a Finnish deployment asks. Found by M9-01.
- **AIS: AISStream asks for `permessage-deflate`; `tungstenite` does not implement it.** The stream works uncompressed; revisit if bandwidth matters or when the crate gains the extension. Found by M9-01.
- **ADS-B: airplanes.live is wired but unverified** — its point endpoint answered `403` to the research probe; marked experimental in the README. Verify from a network that is not blocked, or drop it. Found by M9-02.
- **FIRMS: never run against the live API or a device.** Built where `firms.modaps.eosdis.nasa.gov` was unreachable: confirm the refusal text/status for a bad MAP_KEY, the `days` ceiling and `429` behaviour with a real key, and that ATAK/iTAK/WinTAK/CloudTAK draw the `b-m-p-s-m` + `<color>` marker and the `u-d-f` footprint as intended. Found by M9-10.
- **Three feed plugins now carry their own `SourceState`** (ADS-B and AIS with `notice`, FIRMS a simplified one). Lift one into `rustak_client::feed` when a fourth HTTP feed arrives. Found by M9-10.
- **Neither feed plugin has been run against a live source** (the agents' sandbox blocked outbound TCP); AISStream's handshake and the aggregator polling loop are proven on fixtures and mock servers only. First real run is the maintainer's deployment; watch the Services page for `source.state`.
- **`Sidecar::health` takes no context** (M9-04 followed the trait's style over the brief's signature); if a plugin ever needs the context there, add the parameter in the five places the status note lists.
- **`cot_browser.rs`/`cot_drawer.rs` share the `use_resource` latent trap** M9-03 avoided by keying the services drawer on the service name: `use_resource` re-fetches only on mount/reload, so selecting a second row can show the first row's data. Key those drawers the same way. Found by M9-03.
- **A service-account onboarding action** (M5-03's shape for sidecars): create the service account, mint its service token and a one-time enrolment token in one admin action, shown once, so deploying a plugin is one console step. Suggested by the cluster deployment session while packaging the feed plugins; enrolment tokens expire in 15 minutes (`[auth] enrollment_token_ttl`, server-wide), which is what makes a combined action worth having.
- **`--check` does not validate `[service] control_truststore`** (M9-07): a path that does not exist or holds no certificates is only discovered at the first control call. Validate it where `truststore` is validated. Found by M9-07.
- **The platform-roots half of the sidecar's per-endpoint trust is proven only in deployment**: a test cannot add a platform root and `std::env::set_var` is unsafe under `unsafe_code = "forbid"`, so `sidecar_trust` proves the replacing half and the policy function is unit-tested; the join was confirmed against a Let's Encrypt public listener in the Dublin deployment. If the harness ever gains an injectable root store, close this. Found by M9-07.
- **`auth.resolve.cert` still logs its failure at ERROR via `err(Debug)`**; it is terminal (unlike the bearer path demoted in M9-07), but check whether a routine refusal (unknown or revoked certificate at the HTTP layer) deserves ERROR or `warn`. Found by M9-07.
- **Two groups of tests still depend on host speed** (flagged by M9-13's audit, untouched): the `until(EXPECT)` loops in three suites and every `expect_uid(.., EXPECT)` in `feed_sidecars` allow 5 s from sidecar spawn across register, handshake and first tick — they passed on the slowest CI run seen but are next to fail if runners slow further (raise `EXPECT`, it is only a hung-wait); and M9-11's server-event feed timeout tests in `rustak-client/src/control/events.rs` use REAL timers with 150–300 ms margins (a 150 ms total timeout must outlast loopback connect plus headers), so a scheduling stall of that size fails them — move them onto a paused clock. Rule for every brief: no upper time bound across work whose duration depends on the host; prove counts or ordering, keep timeouts for hung waits only.
- **`docs/plugins.md` does not mention `SidecarContext::stream_stats()` (published / discarded / discarded_before_first_connection) or `with_first_connect_hold`**, both added by M9-13; the rustdoc covers them. One paragraph under the harness section.
- **Make the `workload_identity` suite cheaper.** It is now the most expensive binary in the `Test` job (22 tests, 168–215 s under coverage): every test boots its own server and enrols, and 20–35 s is issuer-key warm-up. Share one server (and one warmed issuer) per suite or group the refusal cases into table-driven tests against a single deployment; same treatment is worth a look for `sidecar_enrolment` (2 → 5 tests), `sidecar_trust` and `hostile_server_name`. Context: the `Test` band moved from 16–20 to 22–28 minutes in three days (per-binary totals 662–869 s → 1113–1253 s), which pushed the sized bound past 60 minutes; the bound was raised to 90 on 2026-09-22 as the cheap half of the answer. Measured by the CI-02 steward.
- **Two wording slips in the sidecar's failure messages** (seen in the Dublin deployment on b485e49, both cosmetic): the CoT stream's first failed connect in a process logs `The TAK stream went away; reconnecting.` although the stream has never been up — same family as the discard WARN M9-11 fixed; say `could not connect yet; retrying` until the first connection (`rustak-client/src/stream/reconnect.rs`). And the advice block under a plain TCP connect timeout (`tcp connect error: deadline has elapsed`) still leads with two truststore/TLS hints, which cannot apply to a connect error: choose the advice from the error chain's class (DNS / connect / TLS / HTTP status) in `rustak-client/src/http.rs::transport`.
- **adsb.lol's limiter looks like a budget over a longer window, not a minimum gap**: on b485e49 the adaptive cadence climbed 10 → 15 → 23 → 35 s in eight minutes with refusals at each rung (3 in the first 7 minutes) while the feed stayed healthy. Wait for the deployment's longer reading before tuning; if it keeps climbing toward the 120 s cap or oscillates, consider a slower multiplicative step, a longer clean run before easing back, or preferring adsb.fi (documented 1 request/s) as the default provider.
- **`interop/cloudtak` still carries the hand-built CSR path it no longer takes.** `src/pki.rs`'s `generateClientRequest`, most of `src/enroll.ts` and `tests/enroll.test.ts` are now the fallback for a server older than M5-03; the suite prefers `POST /api/v1/users/{username}/cloudtak-onboarding` when the surface answers. Worth deleting once no supported release predates it — the parsers are the only offline coverage of `signClient/v2`'s response shape, so they should move rather than go. Found by M5-03.
- **Local toolchain is older than CI's stable** and this machine cannot reach static.rust-lang.org; CI is the authority for new clippy lints until that changes.

- Optional: a dry run for `POST /config-packages`. The only way to find out whether a package can be built for an account is to download one, so the panel has to offer the button before it knows. Left open by M3-06: not worth an endpoint on its own yet.

## Needs a maintainer decision or an upstream release
- **opentelemetry 0.33 (Dependabot #11) is blocked on `tracing-batteries-rs`**, which rustak takes from git and which still depends on 0.32; bumping rustak alone puts two `opentelemetry` versions in scope and fails to compile. Do it upstream first, together with the pending stdout-ANSI change sitting uncommitted in `../tracing-batteries-rs` (M7-05), then `cargo update -p tracing-batteries` and take the bump. Found by the CI steward, 2026-09-21.
- **stdout carries ANSI colour when stdout is not a TTY** (reported by the Nomad deployment session; `docker logs` shows escape codes). `tracing-batteries`' OpenTelemetry battery exposes `with_stdout(bool)` but no colour/`NO_COLOR`/`IsTerminal` knob, so this needs an upstream option in `sierrasoftworks/tracing-batteries-rs` first, then a one-line change in `rustak-core/src/telemetry.rs` to pass `std::io::stdout().is_terminal() && env NO_COLOR is unset`.
- **Shared `target/` fills the disk on macOS**: the dev profile's default `split-debuginfo = "unpacked"` keeps every codegen unit's `.o` file under `target/debug/deps` as the debug-info carrier (509k files, >100 GB after a day of parallel agents). Decide with the user: `[profile.dev] split-debuginfo = "packed"` (dsymutil per binary, small dSYMs, slower links) or `debug = "line-tables-only"` plus periodic `find target/debug/deps -name '*.o' -mmin +60 -delete`. Until then the orchestrator prunes `.o` files older than an hour before each landing.

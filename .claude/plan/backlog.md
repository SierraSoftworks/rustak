# Backlog — small follow-ups recorded by the orchestrator

Items too small for a brief of their own, or waiting for a milestone. Remove a line when it lands.

- **GeoChat bounce (`b-t-f-s`)** — brief `M1-08-chat-bounce.md` exists; launch when `stream/router.rs` is free.
- **`[web.public] plain_bind` is parsed and validated but never bound** — an `http-01` ACME deployment, and the `80 → 443` redirect design 03 §3 describes, both want a plaintext listener on it serving `pki::acme::http01_routes` and `301`ing everything else. (`web/server.rs`, `runtime.rs`.) Found by M2-10.
- **The ACME resolver and the `http-01` token map are process-wide statics** — both become ordinary handles once `services/mod.rs` can take a `Late<AcmeState>` slot. (`services/mod.rs`, `pki/acme/{mod,challenge}.rs`.) Found by M2-10.
- **Wildcard ACME names pass `--check` but no challenge rustak implements can validate one** — a wildcard needs `dns-01`. Either implement it or refuse a wildcard at `--check`. (`config/acme.rs`, `pki/acme/order.rs`.) Found by M2-10.
- **OAuth2 authorize flow and `/login/*` OIDC federation** for CloudTAK SSO — M5 (design 03 M5.1–M5.5).
- **`interop/cloudtak` compose nightly** — M4 gate (plan → Verification).
- **Config-package UI** — the server has `POST /api/v1/config-packages` (M3-02); the admin UI has no page for it yet, nor for packages/profiles/clients/CoT browser (design 04 §8.3).
- **The CloudTAK hand-over has no scheduled sweep of its own.** `identity::cloudtak::sweep` runs when the next hand-over is prepared and the ten-minute expiry is enforced on read, so a stale bundle is never served — but on an installation that onboards CloudTAK once, one sealed keystore sits in `kv` until the next time anybody does. A `jobs/` entry beside `audit_prune` would close it; not done in M5-03 because the job host, `config/jobs.rs` and `jobs/mod.rs` were outside the brief's files. (`identity/cloudtak/mod.rs`, `jobs/`.) Found by M5-03.
- **`interop/cloudtak` still carries the hand-built CSR path it no longer takes.** `src/pki.rs`'s `generateClientRequest`, most of `src/enroll.ts` and `tests/enroll.test.ts` are now the fallback for a server older than M5-03; the suite prefers `POST /api/v1/users/{username}/cloudtak-onboarding` when the surface answers. Worth deleting once no supported release predates it — the parsers are the only offline coverage of `signClient/v2`'s response shape, so they should move rather than go. Found by M5-03.
- **Local toolchain is older than CI's stable** and this machine cannot reach static.rust-lang.org; CI is the authority for new clippy lints until that changes.
- **`rustak-ui`'s unit tests are compiled but never run.** `cargo clippy --all-targets --target wasm32-unknown-unknown` type-checks them and the UI job has no wasm test runner, so an assertion that would fail is not a failing build — roughly twenty tests that cost nothing and prove nothing. `wasm-bindgen-test`, or a host-target `cargo test` for the modules that do not touch `web_sys`. Found by M2-14.
- **A channel an OIDC sign-in creates does not invalidate the routing cache.** `identity::groups::{create,patch,delete}` now tell the stream at once (M2-14); `apply_claims` → `lookup_or_create` still takes `&Database`, because `identity/users.rs` does, so an auto-created channel waits for the sub-second refresh. (`identity/{groups,users}.rs`.) Found by M2-14.
- **Demo mode can only show one TLS source.** `fixtures::certificates::acme_status()` is a failed ACME order, so the `files` rows, note and banner M2-14 added to the Transport security card cannot be seen with `?demo` at all. (`rustak-ui/src/fixtures/certificates.rs`.) Found by M2-14.

## Admin API polish found by the operations UI (M3-04) — closed by M3-06
- Optional: a dry run for `POST /config-packages`. The only way to find out whether a package can be built for an account is to download one, so the panel has to offer the button before it knows. Left open by M3-06: not worth an endpoint on its own yet.
- **stdout carries ANSI colour when stdout is not a TTY** (reported by the Nomad deployment session; `docker logs` shows escape codes). `tracing-batteries`' OpenTelemetry battery exposes `with_stdout(bool)` but no colour/`NO_COLOR`/`IsTerminal` knob, so this needs an upstream option in `sierrasoftworks/tracing-batteries-rs` first, then a one-line change in `rustak-core/src/telemetry.rs` to pass `std::io::stdout().is_terminal() && env NO_COLOR is unset`.
- **Shared `target/` fills the disk on macOS**: the dev profile's default `split-debuginfo = "unpacked"` keeps every codegen unit's `.o` file under `target/debug/deps` as the debug-info carrier (509k files, >100 GB after a day of parallel agents). Decide with the user: `[profile.dev] split-debuginfo = "packed"` (dsymutil per binary, small dSYMs, slower links) or `debug = "line-tables-only"` plus periodic `find target/debug/deps -name '*.o' -mmin +60 -delete`. Until then the orchestrator prunes `.o` files older than an hour before each landing.

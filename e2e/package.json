{
  "name": "rustak-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests for the rustak admin UI, driven against a real server.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
